openapi: 3.1.0

info:
  title: Darkview Contract
  version: 0.1.0
  summary: The single source of truth for every payload that crosses a Darkview process boundary.
  description: |
    Darkview by Astroman — Phase 1 (live-view / EAA).

    AUTHORITY
    This file plus the JSON Schemas it references are the only place a cross-boundary
    type may be defined. TypeScript + Zod are generated from it for web, mobile and
    cloud. Pydantic models are generated from it for the Python Observatory Agent.
    No hand-written duplicate of any type below may exist in any language.

    Generated artifacts live in `packages/contracts/` (TypeScript + Zod) and
    `agent/contracts/` (Pydantic). They are committed and never hand-edited.
    `npm run contracts:check` fails the build if they drift from this file.

    CHANGE PROCESS
    Only the technical lead approves a change here. If a task needs a field that does
    not exist, stop and open a contract issue. Do not invent a private endpoint or a
    local type.

    BOUNDARY THIS CONTRACT PROTECTS
      web / mobile
        | HTTPS + WSS
      Darkview cloud + mission orchestrator
        | authenticated OUTBOUND WSS (the observatory dials out)
      Observatory Agent (Python 3.12)
        | Alpaca HTTP (mount) + ZWO ASI SDK (camera)
      Celestron NexStar 6SE + ZWO ASI585MC

    No schema in this file gives a browser or mobile client a device address, a mount
    credential, a raw ASCOM/Alpaca value or any other way to reach hardware. Clients
    submit intent; the cloud mints the authenticated CommandEnvelope; the agent
    re-validates it locally and may refuse it.

    TIMESTAMPS
    Every `format: date-time` field is an instant in UTC, written with a `Z` suffix.
    RFC 3339 also permits a numeric offset; this contract does not. The generated
    validators enforce it, and that strictness is deliberate rather than incidental.

    One spelling, because an instant on this wire decides when a telescope moves and
    a reader should never have to normalise before comparing. The rule was written
    down after Milestone S1 found the Python agent emitting `+00:00` on every
    message and the cloud refusing all of them -- and, worse, one message stamped in
    the observatory's local zone, which a permissive reader would have accepted
    without anyone noticing.

    PHASE 1 SCOPE
    Live-view / EAA using short exposures and live stacking. This contract deliberately
    contains no long-exposure astrophotography concepts.

    KNOWN OPEN ITEMS (see docs/planning/phase1-issue-decomposition.md)
      - MissionState below is exactly the enumeration in docs/ENGINEERING.md. RESOLVED by
        ADR-004 (2026-08-31): the Build Plan's LOCKED and DELIVERED are not states,
        and its SOLVE_FAILED, LINK_LOST and EXPIRED are carried as MissionFailureReason
        diagnostic detail attached to a docs/ENGINEERING.md state, never as competing states.
      - SafetyEnvelopeConfig.maxAltitudeDegrees has no default and is nullable on
        purpose. `null` means UNMEASURED. It is measured from the physical optical
        train (mount qualification test Q7) and never guessed.
      - The payment provider payload is modelled as opaque. No BOG iPay field has been
        confirmed against provider documentation and none is invented here.

  contact:
    name: Darkview technical lead
  license:
    name: UNLICENSED

servers:
  - url: /api
    description: |
      Darkview cloud API. The absolute base URL is environment configuration, not
      contract. Never hard-code an observatory or device address anywhere.

tags:
  - name: identity
    description: The signed-in user.
  - name: observatory
    description: Public observatory status.
  - name: targets
    description: Operator-approved target catalogue and live visibility.
  - name: booking
    description: Slots, bookings and payment.
  - name: missions
    description: Mission lifecycle and the client command boundary.
  - name: captures
    description: Captures and the user's Collection.
  - name: network
    description: |
      Partner observatories (ADR-013). A telescope somebody else owns, joining by
      installing the agent on their own machine.
  - name: admin
    description: Operator-only. Separately audited.

security:
  - userSession: []

x-darkview-websockets:
  description: |
    WebSocket channels are part of this contract even though OpenAPI 3.1 has no path
    item for them. Their message envelopes are defined in components/schemas and are
    generated into TypeScript, Zod and Pydantic exactly like the HTTP payloads.
  channels:
    - name: agentLink
      url: /ws/agent
      direction: observatory-agent dials out to cloud
      auth: agentDeviceToken
      constraints: |
        Exactly one live connection per observatory. The observatory NEVER accepts an
        inbound connection. Heartbeat every 5 s. Exponential-backoff reconnect.
      outbound:
        summary: agent -> cloud
        $ref: "#/components/schemas/AgentToCloudMessage"
      inbound:
        summary: cloud -> agent
        $ref: "#/components/schemas/CloudToAgentMessage"
      binary: |
        A LiveFrameHeader message is immediately followed by exactly one binary
        WebSocket frame carrying the encoded image bytes. Pixel data is never
        base64-encoded into JSON.
    - name: missionClient
      url: /ws/mission/{missionId}
      direction: cloud to client
      auth: userSession
      outbound:
        summary: cloud -> client
        $ref: "#/components/schemas/MissionChannelMessage"
      inbound:
        summary: client -> cloud
        $ref: "#/components/schemas/MissionClientMessage"
      constraints: |
        Subscription only. A client may not send a CommandEnvelope on this channel.
        Commands go through POST /missions/{missionId}/command so that the cloud mints
        and signs the envelope.

x-darkview-internal:
  description: |
    Calls between Darkview's own cloud processes. Not client surface: no browser or
    mobile client may call these, and the reverse proxy must not route them from the
    internet. Their payloads are still defined here, because they cross a process
    boundary, and are generated into TypeScript and Zod like every other payload.
  routes:
    - name: observatoryTelemetry
      method: GET
      url: /internal/observatories/{observatoryId}/state
      served_by: apps/realtime
      called_by: apps/api, for GET /admin/observatories/{observatoryId}/state (ADR-017)
      auth: realtimeInternalSecret
      response:
        $ref: "#/components/schemas/ObservatoryTelemetrySnapshot"
      constraints: |
        The latest telemetry the connected agent reported, held in memory and
        released when the link closes. Every refusal and every absence -- no or wrong
        secret, unknown observatory, no link, no sample yet -- is the same 404.

paths:
  /auth/register:
    post:
      operationId: register
      tags: [identity]
      summary: Create an account and send a verification link.
      description: |
        ADR-016. Answers 202 whether or not the address already holds an account, so
        this endpoint cannot be used to learn which addresses do. An unverified
        address is sent a fresh link; a verified one is sent nothing. No session is
        issued until the address is verified.

        The link points at the web client, APP_URL/{locale}/verify-email/{token},
        which posts the token to /auth/verify-email.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "202":
          description: Accepted. A verification link is sent if one is owed.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /auth/verify-email:
    post:
      operationId: verifyEmail
      tags: [identity]
      summary: Consume a verification token and sign in.
      description: |
        ADR-016. A token is single-use and expires thirty minutes after it was sent.
        Verifying ends every other session the user held, then issues the session
        cookies described under the userSession security scheme.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyEmailRequest"
      responses:
        "200":
          description: Verified and signed in. Sets the session cookies.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /auth/sign-in:
    post:
      operationId: signIn
      tags: [identity]
      summary: Exchange an email and password for a session.
      description: |
        ADR-016. A wrong email and a wrong password receive the same 401, and take the
        same time. An address that has not been verified receives 403 only once the
        password has been checked. Sets the session cookies on success.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SignInRequest"
      responses:
        "200":
          description: Signed in. Sets the session cookies.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          $ref: "#/components/responses/TooManyRequests"

  /auth/sign-out:
    post:
      operationId: signOut
      tags: [identity]
      summary: End the current session and clear its cookies.
      responses:
        "204":
          description: Signed out.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /me:
    get:
      operationId: getCurrentUser
      tags: [identity]
      summary: The signed-in user.
      responses:
        "200":
          description: The current user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /observatories/{observatoryId}/state:
    get:
      operationId: getObservatoryStatus
      tags: [observatory]
      summary: Public status of one bookable observatory.
      description: |
        Powers the public status page and the live status chip. Contains no device
        address, no credential and no raw driver state.

        ADR-019: answered for an observatory `GET /observatories` lists. An id that
        does not exist and one that is not bookable are one 404, so a customer
        cannot learn by probing ids that a suspended partner node exists.
      security: []
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      responses:
        "200":
          description: Public observatory status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicObservatoryStatus"
        "404":
          $ref: "#/components/responses/NotFound"

  /observatories/{observatoryId}/conditions:
    get:
      operationId: getObservatoryConditions
      tags: [observatory]
      summary: Tonight's viewing forecast at one bookable observatory.
      description: |
        DV-110. An hourly forecast for tonight's bookable hours: the night that has not
        yet reached dawn, narrowed to the hours the observatory offers. Read from
        forecasts the realtime service fetched and stored; never fetched on request.

        **Advisory only.** Conditions never set or clear a weather hold. The operator's
        hold, reported by `GET /observatories/{observatoryId}/state`, stays authoritative.

        An hour with no stored forecast, or one older than the configured maximum age,
        is UNKNOWN with every value null. Unknown is never reported as clear.

        ADR-019: answered for an observatory `GET /observatories` lists. An id that does
        not exist and one that is not bookable are one 404.
      security: []
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      responses:
        "200":
          description: Tonight's viewing conditions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ViewingConditions"
        "404":
          $ref: "#/components/responses/NotFound"

  /observatories:
    get:
      operationId: listBookableObservatories
      tags: [booking]
      summary: The telescopes a customer may book.
      description: |
        ADR-015: a customer books an instrument, and then a time on that instrument.
        This is the set they choose from, and the source of the `observatoryId` that
        `GET /slots` and `POST /bookings` require.

        An observatory is listed exactly when its network node is APPROVED, whatever
        its kind. A first-party observatory is approved the same way a partner one
        is, so there is one rule rather than two. DRAFT, UNDER_REVIEW and SUSPENDED
        nodes are absent -- not listed as unavailable, absent -- because a telescope
        nobody may book is not a choice, and an observatory with no node at all is
        absent for the same reason.

        Being listed says the instrument may be booked, not that it is free tonight
        or online now. Each slot carries its own `unavailableReason` for that.

        Public-safe fields only, on the rule `PublicObservatoryStatus` already
        documents: no coordinates precise enough to be actionable, no device
        identity, no owner.
      security: []
      responses:
        "200":
          description: Every bookable observatory.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookableObservatoryList"

  /targets:
    get:
      operationId: listTargets
      tags: [targets]
      summary: The full operator-approved catalogue.
      security: []
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Catalogue page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TargetPage"

  /targets/tonight:
    get:
      operationId: listTonightTargets
      tags: [targets]
      summary: Catalogue filtered by live ephemeris, at one observatory.
      description: |
        Computed at request time from altitude, Sun angle, Moon separation and angular
        size fit. Never served from a static list.

        Evaluated at one observatory (DV-067). Visibility is a fact about a site, not
        about the catalogue: the same instant is midnight at one telescope and
        afternoon at another, and each has its own horizon and its own measured
        altitude limit. There is no list of what is up "tonight" without a place.

        404 when no bookable observatory has that id, on the same rule `GET /slots`
        applies: unknown and not bookable are one answer.
      security: []
      parameters:
        - name: observatoryId
          in: query
          required: true
          description: An `id` from `GET /observatories`.
          schema:
            type: string
            format: uuid
        - name: at
          in: query
          required: false
          description: Instant to evaluate visibility at. Defaults to now.
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Targets with their current visibility assessment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TonightTargetList"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /targets/{slug}:
    get:
      operationId: getTarget
      tags: [targets]
      summary: One catalogue target.
      security: []
      parameters:
        - $ref: "#/components/parameters/TargetSlug"
      responses:
        "200":
          description: The target.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Target"
        "404":
          $ref: "#/components/responses/NotFound"

  /slots:
    get:
      operationId: listSlots
      tags: [booking]
      summary: Bookable observation slots on one observatory, for a date.
      description: |
        Slots are generated from astronomical darkness, not from fixed office hours,
        and belong to one observatory (ADR-015). There is no list of "the slots"
        without an instrument: two telescopes have two skies, two horizons and two
        sets of hours their owners offered.

        404 when no bookable observatory has that id. Unknown and not-bookable are
        the same answer: whether a suspended partner node exists is not something a
        caller may probe for.
      security: []
      parameters:
        - name: observatoryId
          in: query
          required: true
          description: An `id` from `GET /observatories`.
          schema:
            type: string
            format: uuid
        - name: date
          in: query
          required: true
          description: |
            The observatory's local date, ISO 8601 (YYYY-MM-DD) -- in the
            `timezone` `GET /observatories` reports for it, not the caller's.
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Slots for the requested date.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SlotList"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /bookings:
    get:
      operationId: listBookings
      tags: [booking]
      summary: The signed-in user's bookings.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Booking page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookingPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: createBooking
      tags: [booking]
      summary: Reserve a slot transactionally and open a payment intent.
      description: |
        The slot reservation runs inside a database transaction, and exclusivity is an
        exclusion constraint over the booked interval at that observatory. Exactly one
        of two simultaneous requests for overlapping time on one telescope succeeds;
        the other receives 409.

        `observatoryId` names the telescope (ADR-015). An id that is unknown, or whose
        node is not APPROVED, is 404 -- the same answer `GET /slots` gives it.

        The slot's length decides which targets it can deliver (ADR-015 §2). A target
        whose `expectedMissionMinutes` exceeds `durationMinutes` is 422.

        A reserved slot is held while payment is outstanding. The hold expires, and the
        slot returns to the available list, if payment does not complete in time; the
        `expiresAt` on the returned payment intent is that deadline. A failed or
        abandoned payment releases the slot and creates no mission.

        Send `Idempotency-Key` when retrying. Without it a retry after a timeout can
        reserve a second slot and open a second payment intent.

        **Loyalty** (DV-095). A cash booking is priced at the slot price less the
        customer's tier discount, and `loyaltyPoints` spends points on top at the
        scheme's rate, leaving at least the scheme's minimum payable. Points are taken
        when the slot is held and given back if the hold lapses, the payment fails or
        the booking is refunded. `loyaltyPoints` together with `voucherCode` is 422: a
        booking uses one price reduction. Too few points is 422.

        **With `voucherCode`** (DV-112) the booking is paid by the voucher: it is
        CONFIRMED at once at no charge, its mission is scheduled, and `paymentIntent` is
        null. The voucher is spent in the same transaction as the reservation, so a
        reservation that fails spends nothing and a voucher is never spent twice. A code
        that is unknown, spent, expired or unpaid, or for a different length, is 422.
        Redemption attempts are rate-limited per account.

        **With `useSubscriptionMinutes`** (ADR-022) the booking is paid from the
        signed-in customer's subscription balance: `durationMinutes` minutes are taken,
        the booking is CONFIRMED at once at no charge, its mission is scheduled, and
        `paymentIntent` is null. The minutes are taken in the same transaction as the
        reservation, so a reservation that fails takes nothing, and two bookings racing
        on one balance cannot both spend it. Minutes are spendable while the
        subscription is ACTIVE or PAUSED and its current period has not ended (the
        maintainer's decision of 2026-09-19: a pause stops charging and granting, not
        spending). Too few minutes, no subscription, or no current period, is 422.
        Never together with `voucherCode` or `loyaltyPoints`: a booking uses one price
        reduction. A refunded booking returns its minutes, never money.
      parameters:
        - $ref: "#/components/parameters/IdempotencyKey"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateBookingRequest"
      responses:
        "201":
          description: Booking reserved, awaiting payment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BookingWithPaymentIntent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /loyalty/scheme:
    get:
      operationId: getLoyaltyScheme
      tags: [booking]
      summary: The loyalty club's rules, as configured.
      description: |
        DV-090. Tiers, thresholds, rates and bonuses are configuration data (ADR-008), so
        a client renders them from here rather than hard-coding them.
      security: []
      responses:
        "200":
          description: The loyalty scheme.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyScheme"

  /loyalty:
    get:
      operationId: getMyLoyalty
      tags: [booking]
      summary: The signed-in user's points, tier, referral code and recent ledger.
      description: |
        DV-091. `balance` is what can be spent; `tierPoints` is what the tier is derived
        from, which counts only points earned on settled payments (bonuses and
        adjustments never raise a tier). Points do not expire.
      responses:
        "200":
          description: The loyalty account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyAccount"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /subscription/plans:
    get:
      operationId: listSubscriptionPlans
      tags: [booking]
      summary: The subscription plans on offer.
      description: |
        ADR-022. Plans are rows rather than code, so a price or a grant changes without
        a deployment and a plan is withdrawn by clearing `isAvailable`. An empty list
        means nothing is on sale, which is the state until prices are set.

        Public, like the loyalty scheme: a price list is what somebody reads before
        they have an account.
      security: []
      responses:
        "200":
          description: The available plans, cheapest first.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SubscriptionPlanOption"

  /subscription:
    get:
      operationId: getMySubscription
      tags: [booking]
      summary: The signed-in user's subscription and remaining minutes.
      description: |
        ADR-022. `null` when the user has never subscribed. `minuteBalance` is what a
        booking can spend and is reported whether or not a subscription is currently
        active, because minutes already granted survive a cancellation until they
        expire.
      responses:
        "200":
          description: The subscription, or null.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/Subscription"
                  - type: "null"
        "401":
          $ref: "#/components/responses/Unauthorized"

    post:
      operationId: subscribe
      tags: [booking]
      summary: Subscribe to a monthly plan.
      description: |
        ADR-022. Opens a payment intent for the first period through the same provider
        path as a booking, and saves the instrument the renewal sweep later charges.
        The subscription grants nothing until that payment settles: there is no path
        that grants minutes before money arrives (ADR-022 §6).

        409 when the user already has a subscription that is not CANCELLED or EXPIRED;
        a plan change is a cancellation and a new subscription, not an edit. 422 for a
        plan that is not on sale. 503 when no plan catalogue has been configured on
        this deployment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubscribeRequest"
      responses:
        "201":
          description: Subscription created, awaiting payment for its first period.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubscriptionWithPaymentIntent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /subscription/cancel:
    post:
      operationId: cancelMySubscription
      tags: [booking]
      summary: Stop the subscription renewing.
      description: |
        ADR-022 §9, and the maintainer's decision of 2026-09-19: cancelling refunds
        nothing. A period that has been paid for runs to its end, so the subscription
        stays ACTIVE with `cancelAtPeriodEnd` set and its minutes stay spendable until
        they expire. A subscription with no funded period is CANCELLED at once.

        Idempotent: cancelling an already-cancelling subscription changes nothing and
        answers 200. 404 when the user has never subscribed. 409 when it is already
        CANCELLED or EXPIRED.
      responses:
        "200":
          description: The subscription as it now stands.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /subscription/pause:
    post:
      operationId: pauseMySubscription
      tags: [booking]
      summary: Pause charging and granting.
      description: |
        ADR-022 §9. A pause is the customer's own act: the sweep neither charges nor
        grants while it holds. Minutes already granted stay spendable until the period
        they belong to expires — pausing is not a way to bank them.

        Idempotent. 404 when the user has never subscribed. 409 for a subscription
        that is not ACTIVE.
      responses:
        "200":
          description: The paused subscription.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /subscription/resume:
    post:
      operationId: resumeMySubscription
      tags: [booking]
      summary: Resume a paused subscription.
      description: |
        ADR-022 §9. The subscription becomes ACTIVE again and the sweep charges it at
        the end of its current period. When that period has already passed, the next
        one starts at resumption: `currentPeriodEnd` becomes now and the sweep charges
        on its next pass, for a month the customer can use (maintainer decision of
        2026-09-19).

        It undoes a pause and nothing else. A subscription that is already ending --
        cancelled, or running out the last period it paid for -- is refused rather
        than answered 200, because a 200 would read as an un-cancellation that
        nothing performed.

        Idempotent. 404 when the user has never subscribed. 409 for a subscription
        that is not PAUSED, and for one that is ending.
      responses:
        "200":
          description: The resumed subscription.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /vouchers:
    get:
      operationId: listMyGiftVouchers
      tags: [booking]
      summary: The gift vouchers the signed-in user bought.
      description: |
        DV-112. Never includes a code: codes are stored hashed and reach only the
        email sent when the payment settles. `codeLast4` lets a buyer tell vouchers apart.
      responses:
        "200":
          description: The buyer's vouchers, newest first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GiftVoucherList"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: purchaseGiftVoucher
      tags: [booking]
      summary: Buy a gift voucher for one observation of a given length.
      description: |
        DV-112. Opens a payment intent through the same provider path as a booking. When
        the payment settles the voucher becomes ACTIVE for twelve months and its code is
        emailed to `recipientEmail`, or to the buyer when none is given.

        A voucher pays for exactly one booking of `durationMinutes`, redeemed with
        `voucherCode` on `POST /bookings`. It is not exchangeable for cash: a refunded
        booking a voucher paid for restores the voucher instead (DV-111).

        422 for a length no slot is sold at. 503 when vouchers are not configured on
        this deployment.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateGiftVoucherRequest"
      responses:
        "201":
          description: Voucher created, awaiting payment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GiftVoucherWithPaymentIntent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /bookings/{bookingId}:
    get:
      operationId: getBooking
      tags: [booking]
      summary: One booking owned by the signed-in user.
      parameters:
        - $ref: "#/components/parameters/BookingId"
      responses:
        "200":
          description: The booking.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /bookings/{bookingId}/cancel:
    post:
      operationId: cancelBooking
      tags: [booking]
      summary: Cancel a booking and release its slot.
      parameters:
        - $ref: "#/components/parameters/BookingId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CancelBookingRequest"
      responses:
        "200":
          description: The cancelled booking.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /bookings/{bookingId}/refund:
    post:
      operationId: refundBooking
      tags: [booking]
      summary: Take the refund a lost slot entitles the customer to.
      description: |
        DV-111. Available while the booking's `entitlement.status` is OPEN: its slot
        was lost to weather or to an observatory fault and the customer lost half of
        it or more. The whole amount originally paid is returned -- for a booking that
        was itself a free reschedule, the payment it was rescheduled from. A booking
        a gift voucher paid for restores the voucher, and one subscription minutes
        paid for returns the minutes (ADR-022 §7); neither is paid out. 409 with no
        open entitlement. 503 when the payment's provider has no refund integration
        yet; the entitlement stays open.
      parameters:
        - $ref: "#/components/parameters/BookingId"
      responses:
        "200":
          description: The refunded booking.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /bookings/{bookingId}/reschedule:
    post:
      operationId: rescheduleBooking
      tags: [booking]
      summary: Book a free replacement slot with the entitlement a lost slot gave.
      description: |
        DV-111. Available while the booking's `entitlement.status` is OPEN and before
        `entitlement.expiresAt`. The new booking is on the same telescope, for the same
        length, at no charge, and is confirmed at once. It must be a slot `GET /slots`
        offers. The target may change. 409 with no open entitlement or when the slot is
        taken; 422 for an instant that is not an offered slot, or for a target whose
        `expectedMissionMinutes` exceeds the slot's length (ADR-015 §2).
      parameters:
        - $ref: "#/components/parameters/BookingId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RescheduleBookingRequest"
      responses:
        "201":
          description: The new, confirmed booking.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /payments/webhook:
    post:
      operationId: receivePaymentWebhook
      tags: [booking]
      summary: Payment provider callback.
      description: |
        The provider's body is opaque to this contract. No provider field is invented
        here. The cloud verifies the provider signature, normalises the result and only
        then confirms the booking and creates the mission. A payment success is never
        simulated in a production environment.
      security:
        - paymentWebhookSignature: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PaymentWebhookEnvelope"
      responses:
        "202":
          description: Accepted for processing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentWebhookAck"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /missions:
    get:
      operationId: listMissions
      tags: [missions]
      summary: The signed-in user's upcoming and past missions.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Mission page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionPage"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /missions/{missionId}:
    get:
      operationId: getMission
      tags: [missions]
      summary: One mission owned by the signed-in user.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "200":
          description: The mission.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Mission"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /missions/{missionId}/start:
    post:
      operationId: startMissionSession
      tags: [missions]
      summary: Open the live session and become the single active session owner.
      description: |
        Exactly one active session owner exists at a time, enforced in the cloud and
        again in the agent. Starting a session issues the session identity that every
        subsequent CommandEnvelope must carry.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "200":
          description: Session opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionSession"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /missions/{missionId}/command:
    post:
      operationId: submitMissionCommand
      tags: [missions]
      summary: Submit a bounded client intent for the active mission.
      description: |
        The client submits INTENT ONLY. It never mints a CommandEnvelope: commandId,
        sessionId, userId, issuedAt and expiresAt are all set by the cloud. The cloud
        validates ownership, expiry and the safety envelope, then relays the envelope to
        the agent, which validates it again independently and may refuse it.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MissionCommandRequest"
      responses:
        "202":
          description: Accepted and relayed to the observatory.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionCommandAccepted"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /missions/{missionId}/observation:
    patch:
      operationId: setMissionObservation
      tags: [missions]
      summary: Open or close this session to observers.
      description: |
        Session-owner only. Sessions are private by default (ADR-007); this is the sole
        way one becomes observable. Closing a session detaches any attached observers.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MissionObservationSettings"
      responses:
        "200":
          description: Observation setting applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Mission"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /missions/{missionId}/observers:
    get:
      operationId: listMissionObservers
      tags: [missions]
      summary: List observers attached to this mission.
      description: Visible to the session owner and to operators. Observers see only the count.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "200":
          description: Observers attached.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionObserverList"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

    post:
      operationId: joinMissionAsObserver
      tags: [missions]
      summary: Take a paid view-only seat on a live session.
      description: |
        Attaches the caller as an OBSERVER (ADR-007). Requires a settled Observer Pack
        payment, the mission to be observable, and a free seat.

        An observer receives mission state and the live view and nothing else. It grants
        no command capability and no captures: nothing from this mission enters the
        observer's Collection.

        Fails with MISSION_NOT_OBSERVABLE if the controller has not opted in, and with
        OBSERVER_CAPACITY_REACHED when all seats are taken.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "201":
          description: Observer seat taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionObserver"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "402":
          $ref: "#/components/responses/PaymentRequired"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

    delete:
      operationId: leaveMissionAsObserver
      tags: [missions]
      summary: Release this caller's observer seat.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "204":
          description: Seat released.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /missions/{missionId}/observer-pack:
    post:
      operationId: purchaseObserverPack
      tags: [missions, payments]
      summary: Buy a view-only seat on a live session somebody else controls.
      description: |
        Opens a payment intent for one Observer Pack seat on this mission (ADR-007) and
        holds the seat while the payment is outstanding, exactly as `POST /bookings`
        holds a slot. The seat becomes usable when the provider callback settles the
        payment; `POST /missions/{missionId}/observers` is what attaches to it, and
        answers 402 until then.

        Capacity is counted on packs, not on attached observers. A seat that has been
        paid for belongs to its buyer for the rest of the session: leaving frees the
        connection, never the seat, so a reconnecting observer is never shut out by
        somebody who bought theirs in the meantime.

        One pack per person per mission. Asking again while a hold is outstanding
        returns the same pack and the same intent rather than opening a second payment.

        Fails with MISSION_NOT_OBSERVABLE if the controller has not opted in, with
        OBSERVER_CAPACITY_REACHED when every seat is sold or held, and with CONFLICT if
        the caller is the session's own controller.
      parameters:
        - $ref: "#/components/parameters/MissionId"
      responses:
        "201":
          description: Seat held and a payment intent opened.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObserverPackWithPaymentIntent"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /missions/{missionId}/events:
    get:
      operationId: listMissionEvents
      tags: [missions]
      summary: Correlated event log for one mission.
      parameters:
        - $ref: "#/components/parameters/MissionId"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Mission event page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionEventPage"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /captures:
    get:
      operationId: listCaptures
      tags: [captures]
      summary: The signed-in user's Collection.
      parameters:
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Capture page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CapturePage"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /captures/{captureId}:
    get:
      operationId: getCapture
      tags: [captures]
      summary: One capture owned by the signed-in user.
      parameters:
        - $ref: "#/components/parameters/CaptureId"
      responses:
        "200":
          description: The capture.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Capture"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /captures/{captureId}/download:
    get:
      operationId: getCaptureDownload
      tags: [captures]
      summary: Short-expiry signed download URL.
      description: Object storage buckets are never public. Every download is a signed URL.
      parameters:
        - $ref: "#/components/parameters/CaptureId"
        - name: kind
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/CaptureAssetKind"
      responses:
        "200":
          description: Signed URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CaptureDownload"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /admin/observatories/{observatoryId}/state:
    get:
      operationId: adminGetObservatoryState
      tags: [admin]
      summary: Full operator telemetry for one observatory.
      description: |
        ADR-017. Telemetry is read live from the realtime service, which holds it only
        while the agent is connected. When there is no telemetry to report -- the agent
        is not connected, or the realtime service cannot be reached -- this answers 503
        OBSERVATORY_OFFLINE rather than inventing device health it cannot see. With no
        safety envelope recorded it answers 503 SAFETY_NOT_CONFIGURED. The envelope and
        the missions stay readable through
        /admin/observatories/{observatoryId}/safety-envelope and /admin/missions, which
        do not depend on the link.

        ADR-019: any observatory that exists, bookable or not -- an operator reviewing
        or suspending a partner node has to be able to read it.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      responses:
        "200":
          description: Operator observatory state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorObservatoryState"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "503":
          $ref: "#/components/responses/ServiceUnavailable"

  /admin/observatories/{observatoryId}/mode:
    post:
      operationId: adminSetObservatoryMode
      tags: [admin]
      summary: Switch one observatory between SIMULATED and REAL hardware.
      description: |
        SIMULATED is the default and the normal state. Switching to REAL is an explicit,
        attended operator action outside the normal test workflow: it requires an
        operator role, a written reason and an affirmative attended-presence
        confirmation. No autonomous or background session may perform it. The switch is
        recorded in the audit log.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetObservatoryModeRequest"
      responses:
        "200":
          description: Mode applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OperatorObservatoryState"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/observatories/{observatoryId}/safety-envelope:
    get:
      operationId: adminGetSafetyEnvelope
      tags: [admin]
      summary: One observatory's active safety envelope configuration.
      description: |
        404 when the observatory does not exist or has no envelope recorded.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      responses:
        "200":
          description: Safety envelope.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SafetyEnvelopeConfig"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: adminSetSafetyEnvelope
      tags: [admin]
      summary: Record a measured safety envelope for one observatory.
      description: |
        maxAltitudeDegrees is a measured value from the physical optical train, not a
        configuration preference. Supplying it requires measuredAt and measuredBy. While
        it is null the system is UNMEASURED and every slew command is refused by both
        the cloud and the agent.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SafetyEnvelopeConfig"
      responses:
        "200":
          description: Safety envelope stored.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SafetyEnvelopeConfig"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/observatories/{observatoryId}/weather-hold:
    post:
      operationId: adminSetWeatherHold
      tags: [admin]
      summary: Set or clear an operator weather hold on one observatory.
      description: |
        A hold blocks every new mission start. Affected customers are notified and
        refunded or rescheduled. Phase 1 requires the manual operator hold; an automatic
        sensor-driven hold is only used if a sensor is actually fitted.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/ObservatoryId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetWeatherHoldRequest"
      responses:
        "200":
          description: Weather state applied.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WeatherState"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /admin/override:
    post:
      operationId: adminOverrideCommand
      tags: [admin]
      summary: Operator manual control, separately audited and still safety-bounded.
      description: |
        An override still passes the agent's safety envelope. The Sun exclusion is never
        overridable. Every override is written to the audit log with the operator's
        identity and stated reason.
      security:
        - userSession: [operator]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OperatorOverrideRequest"
      responses:
        "202":
          description: Override relayed to the observatory.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionCommandAccepted"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/missions:
    get:
      operationId: adminListMissions
      tags: [admin]
      summary: All missions, for operations and support.
      security:
        - userSession: [operator]
      parameters:
        - name: state
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/MissionState"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Mission page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MissionPage"
        "403":
          $ref: "#/components/responses/Forbidden"

  /admin/loyalty/adjustments:
    post:
      operationId: adminAdjustLoyaltyPoints
      tags: [admin]
      summary: Add or remove points on a customer's balance, with a reason.
      description: |
        DV-092. Idempotent by `adjustmentId`: repeating a request changes nothing. An
        adjustment never changes `tierPoints`. A removal that would take the balance
        below zero is 409. Audited with the operator and the reason.
      security:
        - userSession: [operator]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoyaltyAdjustmentRequest"
      responses:
        "200":
          description: The customer's account after the adjustment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoyaltyAccount"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/missions/{missionId}/cancel:
    post:
      operationId: adminCancelMission
      tags: [admin]
      summary: Force-cancel a mission and decide its refund treatment.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/MissionId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminCancelMissionRequest"
      responses:
        "200":
          description: The cancelled mission.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Mission"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /admin/targets/{targetId}:
    patch:
      operationId: adminUpdateTarget
      tags: [admin]
      summary: Enable, disable or tune one catalogue target.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/TargetId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdminUpdateTargetRequest"
      responses:
        "200":
          description: The updated target.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Target"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /admin/logs:
    get:
      operationId: adminListAuditEvents
      tags: [admin]
      summary: Correlated audit event stream. The primary debugging tool.
      security:
        - userSession: [operator]
      parameters:
        - name: missionId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: category
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/AuditCategory"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Audit event page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditEventPage"
        "403":
          $ref: "#/components/responses/Forbidden"

  /network/nodes:
    get:
      operationId: listMyNetworkNodes
      tags: [network]
      summary: The signed-in owner's observatories.
      responses:
        "200":
          description: The caller's nodes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNodeList"
        "401":
          $ref: "#/components/responses/Unauthorized"

    post:
      operationId: registerNetworkNode
      tags: [network]
      summary: Register a telescope you own.
      description: |
        Creates the site, the instrument and the node together, in DRAFT. A node in
        DRAFT accepts no customer and commands no hardware: ADR-013 requires a
        qualification an operator granted before a partner telescope may be operated
        by anyone, and DRAFT is the resting state until then.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterNetworkNodeRequest"
      responses:
        "201":
          description: The registered node, in DRAFT.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNode"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /network/nodes/{nodeId}/submit:
    post:
      operationId: submitNetworkNodeForReview
      tags: [network]
      summary: Offer a node for operator review.
      description: |
        DRAFT to UNDER_REVIEW. The owner is saying the telescope is set up and ready
        to be qualified; it is not an approval and changes nothing about what the
        node may do.
      parameters:
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: The node, now under review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNode"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"

  /admin/network/nodes:
    get:
      operationId: adminListNetworkNodes
      tags: [admin]
      summary: The qualification queue, and every node by status.
      description: |
        DV-122. Unscoped by owner, which is what makes it an admin route. Oldest
        first, because this is a queue: the node that has waited longest for review
        is the one an operator should reach first. Keyset-paged, since nodes are
        registered and moved between states while an operator reads it.

        `approvalStatus` narrows it; `UNDER_REVIEW` is the review queue. An
        unrecognised status is refused rather than ignored, so an operator who asked
        for the queue is never silently shown every node.
      security:
        - userSession: [operator]
      parameters:
        - name: approvalStatus
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/NetworkNodeApprovalStatus"
        - $ref: "#/components/parameters/Cursor"
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Node page.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNodePage"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/network/nodes/{nodeId}:
    get:
      operationId: adminGetNetworkNodeReview
      tags: [admin]
      summary: Everything an operator reads before deciding a qualification.
      description: |
        DV-122. What the database knows about each of ADR-013's conditions, beside
        the node itself, so an approval is a decision made against evidence rather
        than a set of boxes. It grants nothing and changes nothing: approving and
        suspending remain the two POSTs below.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      responses:
        "200":
          description: The node, under review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNodeReview"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"

  /admin/network/nodes/{nodeId}/approve:
    post:
      operationId: adminApproveNetworkNode
      tags: [admin]
      summary: Qualify a partner observatory to operate unattended.
      description: |
        ADR-013. Five of the six conditions are things only a person can attest, and
        the operator attests each one explicitly. The sixth -- a measured safety
        envelope for that instrument -- is checked here rather than asserted, because
        the database knows the answer and a checkbox would let an unmeasured
        telescope be approved by clicking.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveNetworkNodeRequest"
      responses:
        "200":
          description: The approved node.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNode"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/network/nodes/{nodeId}/suspend:
    post:
      operationId: adminSuspendNetworkNode
      tags: [admin]
      summary: Return a node to DRAFT, immediately.
      description: |
        The emergency stop for a telescope nobody is standing next to. It is never
        refused on the grounds that the node is busy: a live mission is a reason to
        suspend, not a reason to wait.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SuspendNetworkNodeRequest"
      responses:
        "200":
          description: The suspended node.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkNode"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/network/nodes/{nodeId}/device-token:
    post:
      operationId: adminIssueDeviceToken
      tags: [admin]
      summary: Issue the device token a node's agent connects with.
      description: |
        ADR-020. Operators only, at any approval state: a token lets the agent
        connect, and the link still refuses to operate anything until the node is
        APPROVED. The token is in this response and nowhere else -- only its SHA-256
        is stored -- so it cannot be read back. 409 when the node already has one;
        rotate it instead.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceTokenChangeRequest"
      responses:
        "201":
          description: The token, shown once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceTokenIssued"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/network/nodes/{nodeId}/device-token/rotate:
    post:
      operationId: adminRotateDeviceToken
      tags: [admin]
      summary: Replace a node's device token, cutting off the old one now.
      description: |
        ADR-020. The old token stops authenticating on commit, and a link still
        connected with it is closed. 409 when the node has no token to rotate.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceTokenChangeRequest"
      responses:
        "200":
          description: The new token, shown once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceTokenIssued"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"

  /admin/network/nodes/{nodeId}/device-token/revoke:
    post:
      operationId: adminRevokeDeviceToken
      tags: [admin]
      summary: Revoke a node's device token, immediately.
      description: |
        ADR-013's "revoke its token in one row", and an emergency stop like suspend:
        never refused, never metered, and a live mission on the node is cancelled.
        The link is closed and no agent can connect until a token is issued again.
      security:
        - userSession: [operator]
      parameters:
        - $ref: "#/components/parameters/NodeId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceTokenChangeRequest"
      responses:
        "204":
          description: Revoked. Also answered when the node had no token.
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"


components:
  securitySchemes:
    userSession:
      type: apiKey
      in: cookie
      name: __Host-darkview_session
      description: |
        Customer or operator session credential (ADR-016). Operator-only routes
        additionally require role OPERATOR. This credential never travels to the
        observatory.

        Issued by /auth/sign-in and /auth/verify-email as two cookies, both Secure,
        SameSite=Lax and Path=/: `__Host-darkview_session` (HttpOnly, the credential)
        and `__Host-darkview_csrf` (readable by the page). A session is valid only
        when both are present. Outside production the `__Host-` prefix is dropped,
        because a browser refuses it over plain HTTP. No token is ever returned in a
        response body.

        Every mutating request must carry an Origin header equal to the web client's
        origin; a missing Origin is refused. The API, the realtime service and the
        web client are served from one host.
    agentDeviceToken:
      type: http
      scheme: bearer
      bearerFormat: opaque
      description: |
        Observatory Agent device token, presented on the outbound WSS handshake at
        /ws/agent. It is issued per observatory, stored only on the observatory
        mini-PC and in cloud secret storage, and is never exposed to any client.
    paymentWebhookSignature:
      type: apiKey
      in: header
      name: X-Darkview-Payment-Signature
      description: |
        Provider signature over the raw webhook body. The exact header name and
        algorithm are provider configuration and must be confirmed against the
        provider's own documentation before implementation. Nothing here is invented.
    realtimeInternalSecret:
      type: http
      scheme: bearer
      bearerFormat: opaque
      description: |
        Shared secret the API presents to the realtime service on x-darkview-internal
        routes (ADR-017). Held only in cloud secret storage, separate from AUTH_SECRET
        and STREAM_SIGNING_SECRET so each can be rotated alone. Never sent to a client
        or to the observatory.

  parameters:
    ObservatoryId:
      name: observatoryId
      in: path
      required: true
      description: An `id` from `GET /observatories`, or for an operator any observatory.
      schema:
        type: string
        format: uuid
    NodeId:
      name: nodeId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    Cursor:
      name: cursor
      in: query
      required: false
      description: Opaque forward pagination cursor from the previous page.
      schema:
        type: string
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20
    MissionId:
      name: missionId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    BookingId:
      name: bookingId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    CaptureId:
      name: captureId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TargetId:
      name: targetId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    TargetSlug:
      name: slug
      in: path
      required: true
      schema:
        type: string
        pattern: "^[a-z0-9-]+$"
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: |
        Client-generated key that makes retrying a booking safe. A second request
        carrying a key the signed-in user has already used returns the booking the
        first request created, with its original payment intent, instead of
        reserving a second slot. Keys are scoped to the user, so two people cannot
        collide on one.
      schema:
        type: string
        minLength: 8
        maxLength: 128
        pattern: "^[A-Za-z0-9_.:-]+$"

  responses:
    BadRequest:
      description: Malformed request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Unauthorized:
      description: Not authenticated.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Forbidden:
      description: Authenticated but not permitted.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    NotFound:
      description: Not found, or not owned by the caller.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    PaymentRequired:
      description: Payment is required and has not settled, for example an unpaid Observer Pack seat.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Conflict:
      description: Conflicts with current state, for example a slot already taken or a session already held.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    UnprocessableEntity:
      description: Well-formed but rejected by validation or by the safety envelope.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    TooManyRequests:
      description: Rate limited. The body does not say how much budget remains or when it returns.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    ServiceUnavailable:
      description: A dependency this request needs is not configured or not reachable.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"

  schemas:
    # ---------------------------------------------------------------------------
    # Primitives and errors
    # ---------------------------------------------------------------------------

    Locale:
      type: string
      description: Darkview ships Georgian and English from the first commit.
      enum: [en, ka]

    Currency:
      type: string
      enum: [GEL]

    UserRole:
      type: string
      enum: [USER, OPERATOR]

    ErrorCode:
      type: string
      enum:
        - BAD_REQUEST
        - UNAUTHENTICATED
        - FORBIDDEN
        - NOT_FOUND
        - CONFLICT
        - VALIDATION_FAILED
        - SLOT_UNAVAILABLE
        - PAYMENT_REQUIRED
        - PAYMENT_FAILED
        - SESSION_NOT_OWNER
        - MISSION_NOT_ACTIVE
        - MISSION_NOT_OBSERVABLE
        - OBSERVER_CAPACITY_REACHED
        - OBSERVER_CANNOT_COMMAND
        - OBSERVATORY_OFFLINE
        - WEATHER_HOLD
        - SAFETY_REFUSED
        - SAFETY_NOT_CONFIGURED
        - RATE_LIMITED
        - INTERNAL

    ApiError:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          description: Human-readable, English. Client-facing copy is localised client-side from `code`.
        details:
          type: object
          additionalProperties: true
          description: Optional structured detail. Never contains a secret, device address or credential.

    PageMeta:
      type: object
      additionalProperties: false
      required: [hasMore]
      properties:
        hasMore:
          type: boolean
        nextCursor:
          type: [string, "null"]

    EquatorialCoordinates:
      type: object
      description: J2000 equatorial coordinates. Conversion to apparent coordinates happens at the device boundary inside the agent, never in a client.
      additionalProperties: false
      required: [raHours, decDegrees, epoch]
      properties:
        raHours:
          type: number
          minimum: 0
          exclusiveMaximum: 24
        decDegrees:
          type: number
          minimum: -90
          maximum: 90
        epoch:
          type: string
          enum: [J2000]

    HorizontalCoordinates:
      type: object
      additionalProperties: false
      required: [altitudeDegrees, azimuthDegrees]
      properties:
        altitudeDegrees:
          type: number
          minimum: -90
          maximum: 90
        azimuthDegrees:
          type: number
          minimum: 0
          exclusiveMaximum: 360

    User:
      type: object
      additionalProperties: false
      required: [id, email, role, locale, createdAt]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        displayName:
          type: [string, "null"]
        role:
          $ref: "#/components/schemas/UserRole"
        locale:
          $ref: "#/components/schemas/Locale"
        createdAt:
          type: string
          format: date-time

    RegisterRequest:
      type: object
      additionalProperties: false
      required: [displayName, email, password, locale]
      properties:
        displayName:
          type: string
          minLength: 2
          maxLength: 80
        email:
          type: string
          format: email
          maxLength: 254
        password:
          type: string
          minLength: 12
          maxLength: 128
        locale:
          $ref: "#/components/schemas/Locale"
        referralCode:
          type: string
          minLength: 6
          maxLength: 16
          description: |
            Another member's referral code (DV-096). Both receive the referral bonus once
            this account's first paid booking settles. An unknown code is ignored.

    SignInRequest:
      type: object
      additionalProperties: false
      required: [email, password]
      properties:
        email:
          type: string
          format: email
          maxLength: 254
        password:
          type: string
          minLength: 1
          maxLength: 128

    VerifyEmailRequest:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token:
          type: string
          minLength: 16
          maxLength: 128
          pattern: "^[A-Za-z0-9_-]+$"

    # ---------------------------------------------------------------------------
    # Targets
    # ---------------------------------------------------------------------------

    TargetType:
      type: string
      description: |
        Phase 1 catalogue classes only. Every class in this enum is one the
        NexStar 6SE + ASI585MC combination can present convincingly from a Bortle 8-9
        sky. Wide targets that do not fit any available field of view are excluded from
        the catalogue and therefore from this enum.
      enum:
        - MOON
        - PLANET
        - DOUBLE_STAR
        - GLOBULAR_CLUSTER
        - PLANETARY_NEBULA
        - BRIGHT_NEBULA

    SolarSystemBody:
      type: string
      description: |
        A body whose position is computed from ephemeris at request time. Phase 1
        offers the Moon and the four planets the Build Plan lists; the rest are
        named so the enum does not need a breaking change to add one.
      enum:
        - MOON
        - MERCURY
        - VENUS
        - MARS
        - JUPITER
        - SATURN
        - URANUS
        - NEPTUNE

    TargetPositionSource:
      type: string
      description: |
        Where a target's position comes from. FIXED targets carry J2000
        coordinates. EPHEMERIS targets do not have any: the Moon and the planets
        move, and a stored coordinate for one is a statement that is false the day
        after it is written. Their position is computed at request time from
        solarSystemBody.
      enum: [FIXED, EPHEMERIS]

    OpticalConfig:
      type: string
      description: |
        The three optical configurations of the C6. Focal length and image scale follow
        from the choice. F20_BARLOW = 3000 mm, F10_NATIVE = 1500 mm,
        F6_3_REDUCER = 945 mm with the #94175 reducer.
      enum:
        - F20_BARLOW
        - F10_NATIVE
        - F6_3_REDUCER

    ImagingProfile:
      type: string
      description: |
        Short-exposure / live-stacking profiles only. Phase 1 is a live-view (EAA)
        experience. There is no long-exposure profile and none may be added without an
        approved decision record.
      enum:
        - LUNAR
        - PLANETARY
        - DOUBLE_STAR
        - GLOBULAR_CLUSTER
        - PLANETARY_NEBULA
        - BRIGHT_NEBULA

    Target:
      type: object
      description: |
        A catalogue object. Exactly one of `coordinates` or `solarSystemBody` is
        present, decided by `positionSource`: a FIXED target has coordinates and no
        body, an EPHEMERIS target has a body and no coordinates.
      additionalProperties: false
      required:
        - id
        - slug
        - type
        - nameEn
        - nameKa
        - positionSource
        - angularSizeArcmin
        - magnitude
        - opticalConfig
        - imagingProfile
        - minAltitudeDegrees
        - expectedMissionMinutes
        - enabled
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
          pattern: "^[a-z0-9-]+$"
        catalogId:
          type: [string, "null"]
          description: Catalogue designation, for example "M13" or "NGC 6205".
        type:
          $ref: "#/components/schemas/TargetType"
        nameEn:
          type: string
        nameKa:
          type: string
        descriptionEn:
          type: [string, "null"]
        descriptionKa:
          type: [string, "null"]
        positionSource:
          $ref: "#/components/schemas/TargetPositionSource"
        coordinates:
          oneOf:
            - $ref: "#/components/schemas/EquatorialCoordinates"
            - type: "null"
          description: J2000 coordinates. Present only when positionSource is FIXED.
        solarSystemBody:
          oneOf:
            - $ref: "#/components/schemas/SolarSystemBody"
            - type: "null"
          description: Present only when positionSource is EPHEMERIS.
        angularSizeArcmin:
          type: number
          exclusiveMinimum: 0
        magnitude:
          type: number
        opticalConfig:
          $ref: "#/components/schemas/OpticalConfig"
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        minAltitudeDegrees:
          type: number
          minimum: 0
          maximum: 90
        expectedMissionMinutes:
          type: integer
          exclusiveMinimum: 0
        previewImageUrl:
          type: [string, "null"]
          format: uri
          description: |
            Must be an image this telescope actually produced. Never a stock image and
            never output from a professional instrument presented as Darkview output.
        enabled:
          type: boolean

    TargetPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Target"
        page:
          $ref: "#/components/schemas/PageMeta"

    VisibilityBlockReason:
      type: string
      enum:
        - BELOW_MIN_ALTITUDE
        - ABOVE_MAX_ALTITUDE
        - BEHIND_HORIZON_MASK
        - IN_FORBIDDEN_AZIMUTH
        - SUN_TOO_HIGH
        - TOO_CLOSE_TO_SUN
        - TOO_CLOSE_TO_MOON
        - DOES_NOT_FIT_FIELD
        - BELOW_HORIZON
        - TARGET_DISABLED
        - WEATHER_HOLD
        - OBSERVATORY_OFFLINE
        - SAFETY_ENVELOPE_UNMEASURED

    TargetVisibility:
      type: object
      description: Computed at request time from live ephemeris. Never cached as a static list.
      additionalProperties: false
      required:
        [
          observable,
          evaluatedAt,
          horizontal,
          sunAltitudeDegrees,
          moonSeparationDegrees,
          blockReasons,
        ]
      properties:
        observable:
          type: boolean
        evaluatedAt:
          type: string
          format: date-time
        horizontal:
          $ref: "#/components/schemas/HorizontalCoordinates"
        sunAltitudeDegrees:
          type: number
        moonSeparationDegrees:
          type: number
        risesAt:
          type: [string, "null"]
          format: date-time
        setsAt:
          type: [string, "null"]
          format: date-time
        blockReasons:
          type: array
          items:
            $ref: "#/components/schemas/VisibilityBlockReason"

    TonightTarget:
      type: object
      additionalProperties: false
      required: [target, visibility]
      properties:
        target:
          $ref: "#/components/schemas/Target"
        visibility:
          $ref: "#/components/schemas/TargetVisibility"

    TonightTargetList:
      type: object
      additionalProperties: false
      required: [observatoryId, items, evaluatedAt]
      properties:
        observatoryId:
          type: string
          format: uuid
          description: The observatory every assessment in `items` was made at.
        items:
          type: array
          items:
            $ref: "#/components/schemas/TonightTarget"
        evaluatedAt:
          type: string
          format: date-time

    # ---------------------------------------------------------------------------
    # Observatory state
    # ---------------------------------------------------------------------------

    ObservatoryMode:
      type: string
      description: |
        SIMULATED is the default implementation, always. Any interface fed by the
        simulator must be visibly labelled SIMULATED OBSERVATORY. REAL is reachable
        only through an explicit, attended operator action.
      enum: [SIMULATED, REAL]

    AgentPosture:
      type: string
      description: |
        Who the agent believes is at the instrument, decided by the agent alone (ADR-024).
        A process starts SIMULATED, or ATTENDED when it was started with the attended flag;
        it never starts UNATTENDED. UNATTENDED is armed by a local operator act from
        ATTENDED, for that process only, and the daylight override is refused in it. On
        real drivers arming is refused until a sky sensor is fitted. DISARMED is an
        UNATTENDED agent that latched off on a fault, a suspension or a local disarm; it
        refuses every command but PARK and ABORT until an operator restarts it attended and
        arms it again. Read it beside ObservatoryMode, which says whether the drivers are
        real. The cloud may disarm an agent and never arm one.
      enum: [SIMULATED, ATTENDED, UNATTENDED, DISARMED]

    DisarmReason:
      type: string
      description: Why an UNATTENDED agent moved to DISARMED (ADR-024 §4).
      enum:
        - HARDWARE_FAULT
        - PARK_FAILED
        - LINK_LOST
        - APPROVAL_WITHDRAWN
        - LOCAL_DISARM
        - SKY_SENSOR_STALE

    ObservatoryLinkState:
      type: string
      description: |
        ONLINE = agent connected and heartbeating. DEGRADED = connected but a heartbeat
        or device check is late. OFFLINE = no agent connection.
      enum: [ONLINE, DEGRADED, OFFLINE]

    WeatherStatus:
      type: string
      enum: [CLEAR, CLOUDY, UNSAFE, UNKNOWN]

    WeatherSource:
      type: string
      description: Phase 1 requires OPERATOR at minimum. SENSOR only if a sensor is actually fitted.
      enum: [OPERATOR, SENSOR]

    WeatherState:
      type: object
      additionalProperties: false
      required: [status, source, holdActive, updatedAt]
      properties:
        status:
          $ref: "#/components/schemas/WeatherStatus"
        source:
          $ref: "#/components/schemas/WeatherSource"
        holdActive:
          type: boolean
        note:
          type: [string, "null"]
        updatedAt:
          type: string
          format: date-time

    ForecastSource:
      type: string
      description: The forecast provider that produced a stored hour (DV-110).
      enum: [METEOBLUE, OPEN_METEO]

    ViewingConditionsStatus:
      type: string
      description: UNKNOWN when no forecast is stored for the hour or the stored one is too old.
      enum: [KNOWN, UNKNOWN]

    ViewingConditionsHour:
      type: object
      description: |
        One forecast hour. Every value is null when `status` is UNKNOWN, and any value
        the source does not provide is null when KNOWN. Cloud layers are as the source
        defines them, which differ between providers; `source` says which applies.
      additionalProperties: false
      required:
        - at
        - status
        - source
        - fetchedAt
        - cloudCoverPercent
        - cloudCoverLowPercent
        - cloudCoverMidPercent
        - cloudCoverHighPercent
        - precipitationProbabilityPercent
        - relativeHumidityPercent
        - windSpeedMetresPerSecond
        - seeingArcseconds
      properties:
        at:
          type: string
          format: date-time
          description: The instant the forecast hour begins, on the hour.
        status:
          $ref: "#/components/schemas/ViewingConditionsStatus"
        source:
          oneOf:
            - $ref: "#/components/schemas/ForecastSource"
            - type: "null"
        fetchedAt:
          type: [string, "null"]
          format: date-time
        cloudCoverPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        cloudCoverLowPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        cloudCoverMidPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        cloudCoverHighPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        precipitationProbabilityPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        relativeHumidityPercent:
          type: [number, "null"]
          minimum: 0
          maximum: 100
        windSpeedMetresPerSecond:
          type: [number, "null"]
          minimum: 0
        seeingArcseconds:
          type: [number, "null"]
          minimum: 0
          description: Only from a source with an astronomy seeing forecast.

    ViewingConditions:
      type: object
      additionalProperties: false
      required: [observatoryId, date, items]
      properties:
        observatoryId:
          type: string
          format: uuid
        date:
          type: string
          format: date
          description: The observatory's local date on which tonight begins.
        items:
          type: array
          description: Tonight's bookable hours in order. Empty when the night offers none.
          items:
            $ref: "#/components/schemas/ViewingConditionsHour"

    DeviceHealth:
      type: string
      enum: [OK, DEGRADED, FAULT, DISCONNECTED, NOT_CONFIGURED]

    DeviceStatus:
      type: object
      additionalProperties: false
      required: [health]
      properties:
        health:
          $ref: "#/components/schemas/DeviceHealth"
        detail:
          type: [string, "null"]
          description: Short operator-facing description. Never a device address, port or credential.

    PublicObservatoryStatus:
      type: object
      description: |
        Everything a public page may know. Deliberately excludes coordinates precise
        enough to be actionable, device identity, driver state and any address.
      additionalProperties: false
      required: [observatoryId, mode, link, weather, updatedAt]
      properties:
        observatoryId:
          type: string
          format: uuid
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        link:
          $ref: "#/components/schemas/ObservatoryLinkState"
        weather:
          $ref: "#/components/schemas/WeatherState"
        missionInProgress:
          type: boolean
          default: false
        currentTargetName:
          type: [string, "null"]
          description: Public-safe display name only, and only while a mission owner has opted in.
        lastSuccessfulMissionAt:
          type: [string, "null"]
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ObservatoryTelemetrySnapshot:
      type: object
      description: |
        The latest telemetry the realtime service holds for one connected observatory,
        as served on x-darkview-internal to the API (ADR-017). Internal: never sent to a
        client directly.
      additionalProperties: false
      required: [observatoryId, telemetry, lastHeartbeatAt]
      properties:
        observatoryId:
          type: string
          format: uuid
        telemetry:
          $ref: "#/components/schemas/ObservatoryTelemetry"
        lastHeartbeatAt:
          type: string
          format: date-time
          description: The last message of any kind from the agent's link, on the cloud's clock.

    ObservatoryTelemetry:
      type: object
      description: Operator-grade telemetry. Delivered to the operator console and to the mission channel in reduced form.
      additionalProperties: false
      required: [mode, link, mount, camera, focuser, weather, reportedAt]
      properties:
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        link:
          $ref: "#/components/schemas/ObservatoryLinkState"
        mount:
          $ref: "#/components/schemas/DeviceStatus"
        camera:
          $ref: "#/components/schemas/DeviceStatus"
        focuser:
          $ref: "#/components/schemas/DeviceStatus"
        weather:
          $ref: "#/components/schemas/WeatherState"
        pointingEquatorial:
          oneOf:
            - $ref: "#/components/schemas/EquatorialCoordinates"
            - type: "null"
        pointingHorizontal:
          oneOf:
            - $ref: "#/components/schemas/HorizontalCoordinates"
            - type: "null"
        tracking:
          type: [boolean, "null"]
        parked:
          type: [boolean, "null"]
        slewing:
          type: [boolean, "null"]
        focuserPosition:
          type: [integer, "null"]
        ambientTemperatureC:
          type: [number, "null"]
        agentVersion:
          type: [string, "null"]
        reportedAt:
          type: string
          format: date-time

    OperatorObservatoryState:
      type: object
      additionalProperties: false
      required: [observatoryId, telemetry, safetyEnvelope, updatedAt]
      properties:
        observatoryId:
          type: string
          format: uuid
        telemetry:
          $ref: "#/components/schemas/ObservatoryTelemetry"
        safetyEnvelope:
          $ref: "#/components/schemas/SafetyEnvelopeConfig"
        activeMissionId:
          type: [string, "null"]
          format: uuid
        activeSessionId:
          type: [string, "null"]
          format: uuid
        linkLatencyMs:
          type: [integer, "null"]
        lastHeartbeatAt:
          type: [string, "null"]
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SetObservatoryModeRequest:
      type: object
      additionalProperties: false
      required: [mode, reason, attendedOperatorPresent]
      properties:
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        reason:
          type: string
          minLength: 8
          description: Recorded verbatim in the audit log.
        attendedOperatorPresent:
          type: boolean
          description: |
            Must be true to select REAL. It asserts that a human operator is physically
            present at the observatory for this action. A background or autonomous
            process may never set it.

    # ---------------------------------------------------------------------------
    # Safety envelope
    # ---------------------------------------------------------------------------

    HorizonMaskEntry:
      type: object
      description: One bearing from the compass survey of the installation site.
      additionalProperties: false
      required: [azimuthDegrees, minAltitudeDegrees]
      properties:
        azimuthDegrees:
          type: number
          minimum: 0
          exclusiveMaximum: 360
        minAltitudeDegrees:
          type: number
          minimum: 0
          maximum: 90

    AzimuthSector:
      type: object
      description: Inclusive-start, exclusive-end sector, measured clockwise from north.
      additionalProperties: false
      required: [fromDegrees, toDegrees]
      properties:
        fromDegrees:
          type: number
          minimum: 0
          exclusiveMaximum: 360
        toDegrees:
          type: number
          minimum: 0
          exclusiveMaximum: 360

    SafetyEnvelopeConfig:
      type: object
      description: |
        The numbers that stop the telescope destroying itself. Enforced by pure
        functions in the agent and independently pre-checked by the cloud. Every field
        here is a measured or surveyed value for one physical installation. Nothing in
        this schema may carry a shipped default that would let an unmeasured system
        move.
      additionalProperties: false
      required:
        - observatoryId
        - minAltitudeDegrees
        - maxAltitudeDegrees
        - horizonMask
        - forbiddenAzimuthSectors
        - sunExclusionDegrees
        - daylightLockSunAltitudeDegrees
        - nudgeMaxDegrees
        - nudgeRateDegreesPerSecond
        - slewTimeoutSeconds
        - heartbeatLossSeconds
        - linkDeadSeconds
        - refocusTemperatureDeltaC
        - updatedAt
      properties:
        observatoryId:
          type: string
          format: uuid
        minAltitudeDegrees:
          type: number
          minimum: 0
          maximum: 90
          description: Below this, extinction and local obstructions ruin the image.
        maxAltitudeDegrees:
          type: [number, "null"]
          minimum: 0
          maximum: 90
          description: |
            MAX_ALT_SAFE. MEASURED from the physical optical train, never guessed and
            never defaulted. `null` means UNMEASURED: while it is null, both cloud and
            agent MUST refuse every slew command with SAFETY_NOT_CONFIGURED. Setting it
            requires measuredAt and measuredBy.
        maxAltitudeMeasuredAt:
          type: [string, "null"]
          format: date-time
        maxAltitudeMeasuredBy:
          type: [string, "null"]
          description: Identity of the operator who performed the physical clearance measurement.
        maxAltitudeMeasurementNote:
          type: [string, "null"]
          description: How the value was obtained, including the clearance actually observed.
        horizonMask:
          type: array
          description: Minimum altitude by bearing, built from the site compass survey.
          items:
            $ref: "#/components/schemas/HorizonMaskEntry"
        forbiddenAzimuthSectors:
          type: array
          description: Cable-wrap exclusion sectors. The mount routes the long way round rather than crossing one.
          items:
            $ref: "#/components/schemas/AzimuthSector"
        sunExclusionDegrees:
          type: number
          minimum: 15
          maximum: 180
          description: |
            Hard block on any coordinate within this angle of the Sun, computed
            independently by cloud and agent. Never overridable, including by an
            operator override.

            The minimum is 15 rather than 0 because ADR-013 requires the Sun exclusion
            to be unreachable from any parameter on any path, and a parameter that may
            be set to zero is that path. Cloud and agent each apply the same floor
            again in code, independently, so a stored value below it is raised rather
            than obeyed.
        daylightLockSunAltitudeDegrees:
          type: number
          minimum: -90
          maximum: 0
          description: |
            No customer mission may start while the Sun is above this altitude. An
            operator override is permitted for attended terrestrial testing and remains
            Sun-bounded.

            Bounded to real Sun altitudes, and never above the horizon: a lock set
            higher than 0 is a lock that cannot fire.
        nudgeMaxDegrees:
          type: number
          exclusiveMinimum: 0
          description: Cumulative offset a customer may apply from the locked target before the control re-centres.
        nudgeRateDegreesPerSecond:
          type: number
          exclusiveMinimum: 0
          description: Discrete bounded steps only. No continuous slew is ever exposed to a customer.
        slewTimeoutSeconds:
          type: integer
          exclusiveMinimum: 0
        heartbeatLossSeconds:
          type: integer
          exclusiveMinimum: 0
          description: Heartbeat gap after which capture stops and the operator is alerted.
        linkDeadSeconds:
          type: integer
          exclusiveMinimum: 0
          description: Link gap after which the agent Parks unconditionally, with no cloud involvement.
        refocusTemperatureDeltaC:
          type: number
          exclusiveMinimum: 0
        updatedAt:
          type: string
          format: date-time

    SetWeatherHoldRequest:
      type: object
      additionalProperties: false
      required: [holdActive, status]
      properties:
        holdActive:
          type: boolean
        status:
          $ref: "#/components/schemas/WeatherStatus"
        note:
          type: [string, "null"]

    # ---------------------------------------------------------------------------
    # Slots, bookings, payment
    # ---------------------------------------------------------------------------

    BookableObservatory:
      type: object
      description: |
        One telescope a customer may book, and what a customer needs to choose it.

        ADR-015 leaves how a customer chooses between instruments undecided -- a
        list, a map, a recommendation -- so this carries what any of those needs
        and no more. There are no coordinates: a map is not decided, and precise
        coordinates of somebody else's telescope are not a public field.

        `nameKa` may equal `nameEn`. A partner registers one `siteName`, and
        manufacturing a Georgian name for somebody else's property would be
        inventing data about it.
      additionalProperties: false
      required: [id, slug, kind, nameEn, nameKa, city, countryCode, timezone, telescope]
      properties:
        id:
          type: string
          format: uuid
        slug:
          type: string
        kind:
          $ref: "#/components/schemas/NetworkNodeKind"
        nameEn:
          type: string
        nameKa:
          type: string
        city:
          type: string
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        timezone:
          type: string
          description: |
            IANA zone. `GET /slots` takes a date in this zone, so a client in another
            one needs it to ask for the night it means.
        telescope:
          $ref: "#/components/schemas/BookableTelescope"

    BookableTelescope:
      type: object
      description: |
        The instrument, as a customer would compare two of them. ADR-015: a partner
        is selling *that* telescope, with its aperture, and a customer choosing
        between two needs to see the difference.
      additionalProperties: false
      required: [manufacturer, model, apertureMm, focalLengthMm]
      properties:
        manufacturer:
          type: string
        model:
          type: string
        apertureMm:
          type: number
          exclusiveMinimum: 0
        focalLengthMm:
          type: number
          exclusiveMinimum: 0

    BookableObservatoryList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/BookableObservatory"

    Slot:
      type: object
      additionalProperties: false
      required: [observatoryId, startAt, endAt, durationMinutes, available, priceMinor, currency]
      properties:
        observatoryId:
          type: string
          format: uuid
          description: The telescope this slot is time on (ADR-015).
        startAt:
          type: string
          format: date-time
        endAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          exclusiveMinimum: 0
        available:
          type: boolean
        priceMinor:
          type: integer
          minimum: 0
          description: Price in the minor unit of `currency` (tetri for GEL). Never a float.
        currency:
          $ref: "#/components/schemas/Currency"
        unavailableReason:
          oneOf:
            - $ref: "#/components/schemas/SlotUnavailableReason"
            - type: "null"

    SlotUnavailableReason:
      type: string
      enum:
        - ALREADY_BOOKED
        - OUTSIDE_ASTRONOMICAL_DARKNESS
        - WEATHER_HOLD
        - OBSERVATORY_OFFLINE
        - MAINTENANCE
        - IN_THE_PAST

    SlotList:
      type: object
      additionalProperties: false
      required: [observatoryId, date, items]
      properties:
        observatoryId:
          type: string
          format: uuid
        date:
          type: string
          format: date
        items:
          type: array
          items:
            $ref: "#/components/schemas/Slot"

    BookingStatus:
      type: string
      enum:
        - PENDING_PAYMENT
        - CONFIRMED
        - CANCELLED
        - EXPIRED
        - REFUNDED

    Booking:
      type: object
      additionalProperties: false
      required:
        [
          id,
          userId,
          observatoryId,
          targetId,
          slotStartAt,
          durationMinutes,
          status,
          priceMinor,
          currency,
          createdAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        observatoryId:
          type: string
          format: uuid
          description: The telescope the booking is time on (ADR-015).
        targetId:
          type: string
          format: uuid
        slotStartAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          exclusiveMinimum: 0
        status:
          $ref: "#/components/schemas/BookingStatus"
        priceMinor:
          type: integer
          minimum: 0
        currency:
          $ref: "#/components/schemas/Currency"
        paymentId:
          type: [string, "null"]
          format: uuid
        missionId:
          type: [string, "null"]
          format: uuid
        tierDiscountMinor:
          type: integer
          minimum: 0
          description: What the customer's loyalty tier took off the slot price (DV-095).
        loyaltyPointsRedeemed:
          type: integer
          minimum: 0
          description: Points spent on this booking (DV-095).
        subscriptionMinutesSpent:
          type: integer
          minimum: 0
          description: Subscription minutes that paid for this booking (ADR-022).
        entitlement:
          description: |
            DV-111. Null until the slot has ended and been evaluated, and when nothing
            was lost on our side or less than half the slot was lost.
          oneOf:
            - $ref: "#/components/schemas/BookingEntitlement"
            - type: "null"
        createdAt:
          type: string
          format: date-time

    BookingLossCause:
      type: string
      description: What lost the slot. A customer who did not show up is not a cause.
      enum: [WEATHER, OBSERVATORY_FAULT]

    BookingEntitlement:
      type: object
      description: |
        DV-111, maintainer rules of 2026-09-15. A slot lost to weather or to an
        observatory fault -- internet, power or telescope -- where the customer lost
        half the slot or more. OPEN offers a full refund or a free reschedule until
        `expiresAt`, thirty days after the slot was evaluated; an entitlement still
        OPEN then is refunded automatically.
      additionalProperties: false
      required: [status, cause, minutesLost, expiresAt, rescheduledBookingId]
      properties:
        status:
          type: string
          enum: [OPEN, REFUNDED, RESCHEDULED]
        cause:
          $ref: "#/components/schemas/BookingLossCause"
        minutesLost:
          type: integer
          minimum: 0
        expiresAt:
          type: string
          format: date-time
        rescheduledBookingId:
          type: [string, "null"]
          format: uuid
          description: The replacement booking, once RESCHEDULED.

    RescheduleBookingRequest:
      type: object
      additionalProperties: false
      required: [slotStartAt]
      properties:
        slotStartAt:
          type: string
          format: date-time
          description: A slot `GET /slots` offers on the booking's telescope, of the booking's length.
        targetId:
          type: string
          format: uuid
          description: Defaults to the original booking's target.

    CreateBookingRequest:
      type: object
      additionalProperties: false
      required: [observatoryId, targetId, slotStartAt, durationMinutes]
      properties:
        observatoryId:
          type: string
          format: uuid
          description: An `id` from `GET /observatories`.
        targetId:
          type: string
          format: uuid
        slotStartAt:
          type: string
          format: date-time
        durationMinutes:
          type: integer
          exclusiveMinimum: 0
        locale:
          $ref: "#/components/schemas/Locale"
        voucherCode:
          type: string
          minLength: 8
          maxLength: 64
          description: A gift voucher code (DV-112). Case and separators are ignored.
        loyaltyPoints:
          type: integer
          minimum: 100
          multipleOf: 100
          description: Points to spend on this booking (DV-095). Never with `voucherCode`.
        useSubscriptionMinutes:
          type: boolean
          description: |
            Pay for this booking with subscription minutes (ADR-022). Never with
            `voucherCode` or `loyaltyPoints`.

    CancelBookingRequest:
      type: object
      additionalProperties: false
      properties:
        reason:
          type: string

    PaymentProvider:
      type: string
      description: |
        SANDBOX is the development and CI provider. It is never selectable in a
        production environment and a production payment success is never simulated.
      enum: [SANDBOX, BOG_IPAY]

    PaymentStatus:
      type: string
      enum: [PENDING, AUTHORIZED, CAPTURED, FAILED, REFUNDED]

    PaymentIntent:
      type: object
      description: |
        The minimum a client needs to continue a payment. `redirectUrl` is supplied by
        the provider. No provider-specific field is modelled here because none has been
        confirmed against provider documentation.
      additionalProperties: false
      required: [paymentId, provider, status]
      properties:
        paymentId:
          type: string
          format: uuid
        provider:
          $ref: "#/components/schemas/PaymentProvider"
        status:
          $ref: "#/components/schemas/PaymentStatus"
        redirectUrl:
          type: [string, "null"]
          format: uri
        expiresAt:
          type: [string, "null"]
          format: date-time

    BookingWithPaymentIntent:
      type: object
      additionalProperties: false
      required: [booking, paymentIntent]
      properties:
        booking:
          $ref: "#/components/schemas/Booking"
        paymentIntent:
          description: Null when a gift voucher paid for the booking (DV-112).
          oneOf:
            - $ref: "#/components/schemas/PaymentIntent"
            - type: "null"

    LoyaltyTier:
      type: object
      additionalProperties: false
      required: [code, nameEn, nameKa, thresholdPoints, discountPercent]
      properties:
        code:
          type: string
        nameEn:
          type: string
        nameKa:
          type: string
        thresholdPoints:
          type: integer
          minimum: 0
          description: Purchase-earned points at which the tier begins.
        discountPercent:
          type: integer
          minimum: 0
          maximum: 100

    LoyaltyScheme:
      type: object
      additionalProperties: false
      required:
        - pointsPerGel
        - pointsPerGelRedeemed
        - welcomeBonusPoints
        - referralBonusPoints
        - minimumPayableMinor
        - progressMarkers
        - tiers
      properties:
        pointsPerGel:
          type: integer
          minimum: 0
          description: Points earned per 1 GEL of settled payment.
        pointsPerGelRedeemed:
          type: integer
          exclusiveMinimum: 0
          description: Points that take 1 GEL off a booking.
        welcomeBonusPoints:
          type: integer
          minimum: 0
        referralBonusPoints:
          type: integer
          minimum: 0
        minimumPayableMinor:
          type: integer
          minimum: 0
          description: The least a cash booking may cost after points.
        progressMarkers:
          type: array
          description: Milestones shown on the way to a tier. They grant nothing.
          items:
            type: integer
            minimum: 0
        tiers:
          type: array
          description: Ordered by threshold, lowest first.
          items:
            $ref: "#/components/schemas/LoyaltyTier"

    LoyaltyEntryKind:
      type: string
      enum:
        - WELCOME_BONUS
        - REFERRAL_BONUS
        - PURCHASE_EARNED
        - PURCHASE_REVERSED
        - REDEEMED
        - REDEMPTION_RELEASED
        - ADMIN_ADJUSTMENT

    LoyaltyLedgerEntry:
      type: object
      additionalProperties: false
      required: [id, kind, points, tierPoints, createdAt]
      properties:
        id:
          type: string
          format: uuid
        kind:
          $ref: "#/components/schemas/LoyaltyEntryKind"
        points:
          type: integer
          description: Signed change to the balance.
        tierPoints:
          type: integer
          description: Signed change to tier points. Non-zero only for purchases and their reversal.
        bookingId:
          type: [string, "null"]
          format: uuid
        reason:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time

    SubscriptionPlan:
      type: string
      description: |
        ADR-022. The plan a subscription is on. Prices and grants live in the plan
        catalogue, never in this enum.
      enum: [OBSERVER, EXPLORER, ADVANCED]

    SubscriptionStatus:
      type: string
      description: |
        ADR-022. A failed renewal leaves a subscription ACTIVE while it is retried and
        EXPIRED when the grace period lapses; there is no separate past-due state.
        TRIALING is unused in Phase 1.
      enum: [TRIALING, ACTIVE, PAUSED, CANCELLED, EXPIRED]

    SubscriptionPlanOption:
      type: object
      additionalProperties: false
      required: [plan, nameEn, nameKa, priceMinor, currency, minutesPerPeriod]
      properties:
        plan:
          $ref: "#/components/schemas/SubscriptionPlan"
        nameEn:
          type: string
        nameKa:
          type: string
        priceMinor:
          type: integer
          exclusiveMinimum: 0
          description: Monthly price in the currency's minor unit.
        currency:
          type: string
          minLength: 3
          maxLength: 3
        minutesPerPeriod:
          type: integer
          exclusiveMinimum: 0
          description: |
            Observation minutes granted when a period is paid. Minutes rather than
            observations because the slot length is not settled (ADR-022 §2).

    Subscription:
      type: object
      additionalProperties: false
      required:
        [subscriptionId, plan, status, currentPeriodStart, currentPeriodEnd,
         cancelAtPeriodEnd, minuteBalance, isDemo]
      properties:
        subscriptionId:
          type: string
          format: uuid
        plan:
          $ref: "#/components/schemas/SubscriptionPlan"
        status:
          $ref: "#/components/schemas/SubscriptionStatus"
        currentPeriodStart:
          type: [string, "null"]
          format: date-time
        currentPeriodEnd:
          type: [string, "null"]
          format: date-time
          description: When the next renewal is attempted, and when this period's minutes expire.
        cancelAtPeriodEnd:
          type: boolean
        minuteBalance:
          type: integer
          minimum: 0
          description: Observation minutes a booking can spend now.
        isDemo:
          type: boolean

    SubscribeRequest:
      type: object
      additionalProperties: false
      required: [plan]
      properties:
        plan:
          $ref: "#/components/schemas/SubscriptionPlan"
        locale:
          $ref: "#/components/schemas/Locale"

    SubscriptionWithPaymentIntent:
      type: object
      additionalProperties: false
      required: [subscription, paymentIntent]
      properties:
        subscription:
          $ref: "#/components/schemas/Subscription"
        paymentIntent:
          $ref: "#/components/schemas/PaymentIntent"

    LoyaltyAccount:
      type: object
      additionalProperties: false
      required: [userId, balance, tierPoints, tier, nextTier, referralCode, recentEntries]
      properties:
        userId:
          type: string
          format: uuid
        balance:
          type: integer
          description: Spendable points. Negative only when a refund reversed points already spent.
        tierPoints:
          type: integer
        tier:
          $ref: "#/components/schemas/LoyaltyTier"
        nextTier:
          oneOf:
            - $ref: "#/components/schemas/LoyaltyTier"
            - type: "null"
        referralCode:
          type: string
        recentEntries:
          type: array
          description: The fifty most recent entries, newest first.
          items:
            $ref: "#/components/schemas/LoyaltyLedgerEntry"

    LoyaltyAdjustmentRequest:
      type: object
      additionalProperties: false
      required: [adjustmentId, userId, points, reason]
      properties:
        adjustmentId:
          type: string
          format: uuid
          description: Client-generated. Makes a retried adjustment a no-op.
        userId:
          type: string
          format: uuid
        points:
          type: integer
          not:
            const: 0
        reason:
          type: string
          minLength: 3
          maxLength: 500

    GiftVoucherStatus:
      type: string
      description: |
        DV-112. PENDING_PAYMENT until the payment settles; CANCELLED when it fails.
        EXPIRED is an ACTIVE voucher past `expiresAt`.
      enum: [PENDING_PAYMENT, ACTIVE, REDEEMED, EXPIRED, CANCELLED]

    GiftVoucher:
      type: object
      additionalProperties: false
      required:
        - id
        - status
        - durationMinutes
        - priceMinor
        - currency
        - codeLast4
        - recipientEmail
        - recipientName
        - expiresAt
        - redeemedBookingId
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/GiftVoucherStatus"
        durationMinutes:
          type: integer
          exclusiveMinimum: 0
        priceMinor:
          type: integer
          minimum: 0
        currency:
          $ref: "#/components/schemas/Currency"
        codeLast4:
          type: [string, "null"]
          description: The code's last four characters. Null until the payment settles.
        recipientEmail:
          type: [string, "null"]
          format: email
        recipientName:
          type: [string, "null"]
        expiresAt:
          type: [string, "null"]
          format: date-time
          description: Twelve months from payment. Null until the payment settles.
        redeemedBookingId:
          type: [string, "null"]
          format: uuid
        createdAt:
          type: string
          format: date-time

    GiftVoucherList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/GiftVoucher"

    GiftVoucherWithPaymentIntent:
      type: object
      additionalProperties: false
      required: [voucher, paymentIntent]
      properties:
        voucher:
          $ref: "#/components/schemas/GiftVoucher"
        paymentIntent:
          $ref: "#/components/schemas/PaymentIntent"

    CreateGiftVoucherRequest:
      type: object
      additionalProperties: false
      required: [durationMinutes]
      properties:
        durationMinutes:
          type: integer
          exclusiveMinimum: 0
          description: The length of the observation the voucher pays for. Must be a length slots are sold at.
        recipientEmail:
          type: string
          format: email
          maxLength: 254
          description: Where the code is emailed. The buyer when omitted.
        recipientName:
          type: string
          minLength: 1
          maxLength: 100
        message:
          type: string
          minLength: 1
          maxLength: 500
          description: A personal note included in the email.

    BookingPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Booking"
        page:
          $ref: "#/components/schemas/PageMeta"

    PaymentWebhookEnvelope:
      type: object
      description: |
        The provider's body is opaque on purpose. The cloud verifies the signature over
        the raw body, then maps the payload to Darkview's own Payment record. Do not add
        provider fields to this schema before they are confirmed against the provider's
        published documentation.
      additionalProperties: false
      required: [provider, payload]
      properties:
        provider:
          $ref: "#/components/schemas/PaymentProvider"
        payload:
          type: object
          additionalProperties: true
          description: Provider-defined, unvalidated by Darkview beyond signature verification.

    PaymentWebhookAck:
      type: object
      additionalProperties: false
      required: [received]
      properties:
        received:
          type: boolean

    # ---------------------------------------------------------------------------
    # Mission
    # ---------------------------------------------------------------------------

    MissionState:
      type: string
      description: |
        The authoritative mission state machine, exactly as enumerated in docs/ENGINEERING.md.

        Primary:  REQUESTED -> SCHEDULED -> PREPARING -> SLEWING -> VERIFYING ->
                  CENTERING -> OBSERVING -> CAPTURING -> PROCESSING -> COMPLETE
        Failure / hold: WEATHER_HOLD, NOT_VISIBLE, HARDWARE_ERROR, CANCELLED, FAILED

        VERIFYING is the ASTAP plate-solve step. CENTERING applies the solved offset and
        re-slews, bounded to three iterations. Any state may enter a failure or hold
        state, and every path out of one ends at Park.

        Resolved by docs/decisions/ADR-004-mission-state-machine.md (2026-08-31). This
        enum matches docs/ENGINEERING.md and nothing else. The Build Plan's LOCKED and DELIVERED
        are not states -- "Target locked" and "LIVE" are display labels mapped in the
        web layer. Its SOLVE_FAILED, LINK_LOST and EXPIRED are carried as
        MissionFailureReason detail on a state above, never as states.
      enum:
        - REQUESTED
        - SCHEDULED
        - PREPARING
        - SLEWING
        - VERIFYING
        - CENTERING
        - OBSERVING
        - CAPTURING
        - PROCESSING
        - COMPLETE
        - WEATHER_HOLD
        - NOT_VISIBLE
        - HARDWARE_ERROR
        - CANCELLED
        - FAILED

    MissionFailureReason:
      type: string
      description: |
        Diagnostic detail attached to a failure or hold state. This is NOT a state and
        must never be used as one, mapped into MissionState, or displayed as a stage in
        a progress indicator.
      enum:
        - PLATE_SOLVE_FAILED
        - SLEW_TIMEOUT
        - CENTERING_ITERATIONS_EXHAUSTED
        - TRACKING_LOST
        - MOUNT_FAULT
        - CAMERA_FAULT
        - FOCUSER_FAULT
        - AGENT_LINK_LOST
        - HEARTBEAT_LOST
        - SAFETY_REFUSED
        - SAFETY_ENVELOPE_UNMEASURED
        - TARGET_SET_BELOW_LIMIT
        - WEATHER_UNSAFE
        - SESSION_EXPIRED
        - OPERATOR_ABORT
        - CUSTOMER_CANCELLED
        - PAYMENT_FAILED

    Mission:
      type: object
      additionalProperties: false
      required: [id, userId, bookingId, targetId, observatoryId, state, mode, requestedAt]
      properties:
        id:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        bookingId:
          type: [string, "null"]
          format: uuid
          description: |
            The booking this mission was sold from, or null. Required so it is always
            stated, nullable because not every mission is sold: an operator mission and
            a demo mission have no booking, and inventing one for them would put a
            fiction in the payment tables.
        targetId:
          type: string
          format: uuid
        observatoryId:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/MissionState"
        failureReason:
          oneOf:
            - $ref: "#/components/schemas/MissionFailureReason"
            - type: "null"
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        scheduledStartAt:
          type: [string, "null"]
          format: date-time
        requestedAt:
          type: string
          format: date-time
        startedAt:
          type: [string, "null"]
          format: date-time
        endedAt:
          type: [string, "null"]
          format: date-time
        captureIds:
          type: array
          items:
            type: string
            format: uuid
        observable:
          type: boolean
          default: false
          description: |
            Whether the controller has opted this session in to being observed. Sessions
            are PRIVATE by default (ADR-007). Only the session owner may change it.
        observerCapacity:
          type: integer
          minimum: 0
          maximum: 5
          default: 5
          description: Hard server-side cap on concurrent observers. Never exceeds 5 (ADR-007).
        observerCount:
          type: integer
          minimum: 0
          description: Observers currently attached. Never exceeds observerCapacity.

    MissionPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Mission"
        page:
          $ref: "#/components/schemas/PageMeta"

    SessionRole:
      type: string
      description: |
        Who a person is on a live mission. Exactly one CONTROLLER exists at a time -- that
        is the session owner, and the only identity for which the cloud will mint a
        CommandEnvelope. An OBSERVER watches and can never command: the agent rejects any
        envelope whose sessionId is not the current owner, independently of the cloud.
      enum: [CONTROLLER, OBSERVER]

    MissionObserver:
      type: object
      description: A paid, view-only seat on a session somebody else controls (ADR-007).
      additionalProperties: false
      required: [id, missionId, userId, joinedAt]
      properties:
        id:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        joinedAt:
          type: string
          format: date-time
        leftAt:
          type: [string, "null"]
          format: date-time

    MissionObserverList:
      type: object
      additionalProperties: false
      required: [items, capacity]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/MissionObserver"
        capacity:
          type: integer
          minimum: 0
          maximum: 5

    ObserverPackStatus:
      type: string
      description: |
        PENDING_PAYMENT holds a seat while the payment is outstanding and lapses at
        `holdExpiresAt` if nothing settles. PAID is a seat its buyer keeps for the rest
        of the session. CANCELLED is a payment that failed; EXPIRED is a hold that ran
        out. Both put the seat back on sale.
      enum: [PENDING_PAYMENT, PAID, CANCELLED, EXPIRED]

    ObserverPack:
      type: object
      description: |
        One purchased, view-only seat on a session somebody else controls (ADR-007).

        The pack is the sale; `MissionObserver` is the attachment to it. They are
        separate because a seat outlives a connection: an observer whose phone drops
        still owns what they paid for.
      additionalProperties: false
      required: [id, missionId, userId, status, priceMinor, currency, createdAt]
      properties:
        id:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/ObserverPackStatus"
        priceMinor:
          type: integer
          minimum: 0
        currency:
          $ref: "#/components/schemas/Currency"
        paymentId:
          type: [string, "null"]
          format: uuid
        holdExpiresAt:
          type: [string, "null"]
          format: date-time
          description: When an unpaid hold lapses. Null once the pack is no longer holding one.
        createdAt:
          type: string
          format: date-time

    ObserverPackWithPaymentIntent:
      type: object
      additionalProperties: false
      required: [observerPack, paymentIntent]
      properties:
        observerPack:
          $ref: "#/components/schemas/ObserverPack"
        paymentIntent:
          $ref: "#/components/schemas/PaymentIntent"

    MissionObservationSettings:
      type: object
      description: |
        The controller's consent to being observed. Set only by the session owner.
        Sessions are private by default and become observable only by this call.
      additionalProperties: false
      required: [observable]
      properties:
        observable:
          type: boolean

    MissionEventSource:
      type: string
      enum: [CLOUD, AGENT, OPERATOR]

    MissionEvent:
      type: object
      description: One correlated entry in the mission audit trail. Never fabricated or backdated.
      additionalProperties: false
      required: [id, missionId, at, state, source]
      properties:
        id:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        at:
          type: string
          format: date-time
        state:
          $ref: "#/components/schemas/MissionState"
        failureReason:
          oneOf:
            - $ref: "#/components/schemas/MissionFailureReason"
            - type: "null"
        source:
          $ref: "#/components/schemas/MissionEventSource"
        commandId:
          type: [string, "null"]
          format: uuid
        detail:
          type: [string, "null"]

    MissionEventPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/MissionEvent"
        page:
          $ref: "#/components/schemas/PageMeta"

    MissionSession:
      type: object
      description: |
        Proof that this client is the single active session owner. The session
        identifier is minted by the cloud and is required on every CommandEnvelope. It
        is not a device credential and gives no access to hardware.
      additionalProperties: false
      required: [sessionId, missionId, userId, issuedAt, expiresAt, missionChannelUrl]
      properties:
        sessionId:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        missionChannelUrl:
          type: string
          description: Relative WSS path for this mission's client channel.
        allowedCommands:
          type: array
          items:
            $ref: "#/components/schemas/ClientCommandType"

    # ---------------------------------------------------------------------------
    # Commands
    # ---------------------------------------------------------------------------

    CommandType:
      type: string
      description: |
        The complete set of instruction types the cloud may send to the agent. A client
        may only cause a subset of these to be minted; see ClientCommandType.
      enum:
        - GOTO
        - NUDGE
        - CAPTURE
        - FOCUS
        - ABORT
        - PARK
        - SET_PROFILE

    ClientCommandType:
      type: string
      description: |
        What a customer may ask for during a live observation. GOTO, FOCUS, PARK and
        SET_PROFILE are never client-initiated: the target comes from the booking and
        the rest are cloud or operator concerns. RECENTER is translated by the cloud
        into a GOTO back to the booked target; it is not a free-form slew.
      enum:
        - NUDGE
        - CAPTURE
        - RECENTER
        - ABORT

    NudgeAxis:
      type: string
      enum: [ALTITUDE, AZIMUTH]

    NudgeDirection:
      type: string
      enum: [POSITIVE, NEGATIVE]

    FocusMode:
      type: string
      enum: [AUTOFOCUS, ABSOLUTE]

    RegionOfInterest:
      type: object
      additionalProperties: false
      required: [x, y, width, height]
      properties:
        x:
          type: integer
          minimum: 0
        y:
          type: integer
          minimum: 0
        width:
          type: integer
          exclusiveMinimum: 0
        height:
          type: integer
          exclusiveMinimum: 0

    GotoPayload:
      type: object
      additionalProperties: false
      required: [kind, targetId, coordinates, opticalConfig, imagingProfile]
      properties:
        kind:
          type: string
          enum: [GOTO]
        targetId:
          type: string
          format: uuid
        coordinates:
          $ref: "#/components/schemas/EquatorialCoordinates"
        opticalConfig:
          $ref: "#/components/schemas/OpticalConfig"
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        recenter:
          type: boolean
          default: false
          description: True when this GOTO returns the mount to the booked target after customer nudges.

    NudgePayload:
      type: object
      description: One discrete bounded step. Never a continuous slew.
      additionalProperties: false
      required: [kind, axis, direction, stepArcminutes]
      properties:
        kind:
          type: string
          enum: [NUDGE]
        axis:
          $ref: "#/components/schemas/NudgeAxis"
        direction:
          $ref: "#/components/schemas/NudgeDirection"
        stepArcminutes:
          type: number
          exclusiveMinimum: 0
          description: Bounded by SafetyEnvelopeConfig.nudgeMaxDegrees cumulatively and by nudgeRateDegreesPerSecond instantaneously.

    CapturePayload:
      type: object
      description: |
        Phase 1 capture is a short-exposure / live-stacking run, not a long exposure.
        The requested integration is bounded by the imaging profile.
      additionalProperties: false
      required: [kind, imagingProfile]
      properties:
        kind:
          type: string
          enum: [CAPTURE]
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        requestedFrames:
          type: [integer, "null"]
          exclusiveMinimum: 0
        targetIntegrationSeconds:
          type: [number, "null"]
          exclusiveMinimum: 0

    FocusPayload:
      type: object
      additionalProperties: false
      required: [kind, mode]
      properties:
        kind:
          type: string
          enum: [FOCUS]
        mode:
          $ref: "#/components/schemas/FocusMode"
        absolutePosition:
          type: [integer, "null"]
          description: Required when mode is ABSOLUTE. The agent applies backlash compensation with a same-direction final approach.

    AbortPayload:
      type: object
      additionalProperties: false
      required: [kind]
      properties:
        kind:
          type: string
          enum: [ABORT]
        reason:
          type: [string, "null"]

    ParkPayload:
      type: object
      description: Park is the answer to every unresolved condition.
      additionalProperties: false
      required: [kind]
      properties:
        kind:
          type: string
          enum: [PARK]
        reason:
          type: [string, "null"]

    SetProfilePayload:
      type: object
      additionalProperties: false
      required: [kind, imagingProfile]
      properties:
        kind:
          type: string
          enum: [SET_PROFILE]
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        exposureMilliseconds:
          type: [number, "null"]
          exclusiveMinimum: 0
          description: |
            Short exposures only. Alt-azimuth field rotation, not the sensor, sets the
            ceiling. Values beyond the profile's documented range are refused.
        gain:
          type: [integer, "null"]
          minimum: 0
        regionOfInterest:
          oneOf:
            - $ref: "#/components/schemas/RegionOfInterest"
            - type: "null"

    CommandPayload:
      description: |
        Discriminated by `kind`. `payload.kind` MUST equal the envelope's `type`; both
        the cloud and the agent reject an envelope where they disagree.
      oneOf:
        - $ref: "#/components/schemas/GotoPayload"
        - $ref: "#/components/schemas/NudgePayload"
        - $ref: "#/components/schemas/CapturePayload"
        - $ref: "#/components/schemas/FocusPayload"
        - $ref: "#/components/schemas/AbortPayload"
        - $ref: "#/components/schemas/ParkPayload"
        - $ref: "#/components/schemas/SetProfilePayload"
      discriminator:
        propertyName: kind
        mapping:
          GOTO: "#/components/schemas/GotoPayload"
          NUDGE: "#/components/schemas/NudgePayload"
          CAPTURE: "#/components/schemas/CapturePayload"
          FOCUS: "#/components/schemas/FocusPayload"
          ABORT: "#/components/schemas/AbortPayload"
          PARK: "#/components/schemas/ParkPayload"
          SET_PROFILE: "#/components/schemas/SetProfilePayload"

    CommandEnvelope:
      type: object
      description: |
        The only structure by which anything may instruct the observatory.

        Minted exclusively by the Darkview cloud. A browser or mobile client never
        constructs one and never sees a device address inside one.

        The agent enforces all of the following independently of the cloud:
          - idempotent by commandId; a repeat of a seen commandId is ignored;
          - rejected after expiresAt;
          - rejected if sessionId is not the current session owner;
          - rejected if userId does not match the session owner;
          - rejected if missionId is not the mission the agent currently holds;
          - rejected if the safety envelope refuses it, even when the cloud approved it.
      additionalProperties: false
      required:
        [commandId, missionId, sessionId, userId, issuedAt, expiresAt, type, payload]
      properties:
        commandId:
          type: string
          format: uuid
          description: Idempotency key. The agent keeps a seen-set so retries are safe.
        missionId:
          type: string
          format: uuid
        sessionId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
          description: Short-lived. A queued command must not fire after a reconnect.
        type:
          $ref: "#/components/schemas/CommandType"
        payload:
          $ref: "#/components/schemas/CommandPayload"
        issuedByOperatorId:
          type: [string, "null"]
          format: uuid
          description: Set only for an operator override. Present on every separately audited command.
        overrideReason:
          type: [string, "null"]
          description: Required when issuedByOperatorId is set. Recorded verbatim in the audit log.

    MissionCommandRequest:
      type: object
      description: |
        Client intent. Deliberately does NOT contain commandId, sessionId, userId,
        issuedAt or expiresAt: the cloud sets all of those. A client that supplies them
        is rejected.
      additionalProperties: false
      required: [type]
      properties:
        type:
          $ref: "#/components/schemas/ClientCommandType"
        nudge:
          oneOf:
            - $ref: "#/components/schemas/NudgePayload"
            - type: "null"
        capture:
          oneOf:
            - $ref: "#/components/schemas/CapturePayload"
            - type: "null"
        reason:
          type: [string, "null"]

    CommandAcceptanceStatus:
      type: string
      enum: [ACCEPTED, REJECTED, DUPLICATE, EXPIRED, COMPLETED, FAILED]

    CommandRejectionReason:
      type: string
      description: |
        Why a command was refused. The SAFETY_ prefixed values are produced by the
        agent's own envelope check and may occur even when the cloud approved the
        command; that is the design, not a bug.
      enum:
        - DUPLICATE_COMMAND_ID
        - COMMAND_EXPIRED
        - WRONG_SESSION
        - WRONG_USER
        - WRONG_MISSION
        - NO_ACTIVE_MISSION
        - MISSION_ALREADY_ACTIVE
        - PAYLOAD_TYPE_MISMATCH
        - MALFORMED_PAYLOAD
        - COMMAND_NOT_PERMITTED_FOR_CLIENT
        - SAFETY_ENVELOPE_UNMEASURED
        - SAFETY_BELOW_MIN_ALTITUDE
        - SAFETY_ABOVE_MAX_ALTITUDE
        - SAFETY_HORIZON_MASK
        - SAFETY_FORBIDDEN_AZIMUTH
        - SAFETY_SUN_EXCLUSION
        - SAFETY_DAYLIGHT_LOCK
        - SAFETY_NUDGE_LIMIT_EXCEEDED
        - SAFETY_SLEW_RATE
        - DEVICE_UNAVAILABLE
        - MODE_NOT_PERMITTED
        - WEATHER_HOLD_ACTIVE
        - OBSERVATORY_OFFLINE
        - UNATTENDED_DISARMED

    MissionCommandAccepted:
      type: object
      additionalProperties: false
      required: [commandId, missionId, type, issuedAt, expiresAt, status]
      properties:
        commandId:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        type:
          $ref: "#/components/schemas/CommandType"
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        status:
          $ref: "#/components/schemas/CommandAcceptanceStatus"
        rejectionReason:
          oneOf:
            - $ref: "#/components/schemas/CommandRejectionReason"
            - type: "null"

    OperatorOverrideRequest:
      type: object
      description: |
        Manual operator control. Still safety-bounded; the Sun exclusion is never
        overridable. Requires a stated reason, which is stored in the audit log.
      additionalProperties: false
      required: [type, payload, reason]
      properties:
        missionId:
          type: [string, "null"]
          format: uuid
          description: Null for a standalone maintenance action outside any mission.
        type:
          $ref: "#/components/schemas/CommandType"
        payload:
          $ref: "#/components/schemas/CommandPayload"
        reason:
          type: string
          minLength: 8

    # ---------------------------------------------------------------------------
    # Captures
    # ---------------------------------------------------------------------------

    CaptureAssetKind:
      type: string
      description: |
        IMAGE is the delivered, stretched, watermarked image. FITS is the real frame
        data. UNMARKED is the stored copy without the overlay. THUMBNAIL is a small
        preview of the same picture, for a Collection card; it is what
        `Capture.thumbnailUrl` signs.
      enum: [IMAGE, THUMBNAIL, FITS, UNMARKED]

    CaptureVisibility:
      type: string
      description: PRIVATE by default. Publishing to the public gallery is opt-in.
      enum: [PRIVATE, GALLERY]

    Capture:
      type: object
      additionalProperties: false
      required:
        - id
        - missionId
        - userId
        - targetId
        - capturedAt
        - imagingProfile
        - opticalConfig
        - exposureMilliseconds
        - gain
        - framesStacked
        - integrationSeconds
        - fitsAvailable
        - visibility
        - mode
      properties:
        id:
          type: string
          format: uuid
        missionId:
          type: string
          format: uuid
        userId:
          type: string
          format: uuid
        targetId:
          type: string
          format: uuid
        capturedAt:
          type: string
          format: date-time
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        opticalConfig:
          $ref: "#/components/schemas/OpticalConfig"
        exposureMilliseconds:
          type: number
          exclusiveMinimum: 0
        gain:
          type: integer
          minimum: 0
        framesStacked:
          type: integer
          exclusiveMinimum: 0
        integrationSeconds:
          type: number
          exclusiveMinimum: 0
        widthPx:
          type: [integer, "null"]
        heightPx:
          type: [integer, "null"]
        solvedFocalLengthMm:
          type: [number, "null"]
          description: Read back from the plate solve. Confirms which optical configuration was actually in the train.
        fitsAvailable:
          type: boolean
        visibility:
          $ref: "#/components/schemas/CaptureVisibility"
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
          description: |
            A capture produced by the simulator is permanently marked SIMULATED. It is
            never presented as telescope output and never used as audit evidence of real
            hardware.
        thumbnailUrl:
          type: [string, "null"]
          format: uri
          description: Signed, short-expiry URL. Never a public bucket path.

    CapturePage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Capture"
        page:
          $ref: "#/components/schemas/PageMeta"

    CaptureDownload:
      type: object
      additionalProperties: false
      required: [kind, url, expiresAt]
      properties:
        kind:
          $ref: "#/components/schemas/CaptureAssetKind"
        url:
          type: string
          format: uri
        expiresAt:
          type: string
          format: date-time

    # ---------------------------------------------------------------------------
    # Audit
    # ---------------------------------------------------------------------------

    # ---------------------------------------------------------------------------
    # Partner observatories (ADR-013)
    # ---------------------------------------------------------------------------

    NetworkNodeKind:
      type: string
      description: |
        FIRST_PARTY is an observatory Darkview owns and operates. PARTNER is one
        somebody else owns, running the same agent under a qualification an operator
        granted and can revoke.
      enum: [FIRST_PARTY, PARTNER]

    NetworkNodeApprovalStatus:
      type: string
      description: |
        DRAFT is the resting state and it refuses everything. UNDER_REVIEW is the
        owner saying the telescope is ready to be qualified; it grants nothing.
        APPROVED is the only state in which a partner observatory may be operated.
        SUSPENDED is an operator having taken that away.

        SUSPENDED and DRAFT both refuse everything, and they are kept distinct
        because they are different facts about a telescope: one has never been
        qualified, the other was and had it revoked. Collapsing them would lose
        exactly the history an operator needs when deciding whether to approve it
        again.
      enum: [DRAFT, UNDER_REVIEW, APPROVED, SUSPENDED]

    NetworkNode:
      type: object
      description: |
        One observatory in the network, and the terms on which it may be used.

        ADR-013: partner status widens who may host a telescope, never what a
        telescope may be asked to do. A node carries no authority of its own -- the
        agent still re-validates every command, still enforces its own safety
        envelope after the link dies, and still refuses a cloud-approved command
        that fails local safety.
      additionalProperties: false
      required:
        [nodeId, observatoryId, ownerId, kind, approvalStatus, siteName, city,
         countryCode, timezone, safetyEnvelopeMeasured, capabilities, agentPosture,
         agentDisarmReason, createdAt]
      properties:
        nodeId:
          type: string
          format: uuid
        observatoryId:
          type: string
          format: uuid
        ownerId:
          type: string
          format: uuid
        kind:
          $ref: "#/components/schemas/NetworkNodeKind"
        approvalStatus:
          $ref: "#/components/schemas/NetworkNodeApprovalStatus"
        siteName:
          type: string
        city:
          type: string
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        timezone:
          type: string
        safetyEnvelopeMeasured:
          type: boolean
          description: |
            Whether a measured altitude limit exists for this instrument. Reported
            rather than asserted: it is the condition approval checks for itself,
            because the database knows the answer and a checkbox would let an
            unmeasured telescope be approved by clicking.
        capabilities:
          type: array
          items:
            type: string
          description: Populated at qualification. Empty at registration.
        agentPosture:
          oneOf:
            - $ref: "#/components/schemas/AgentPosture"
            - type: "null"
          description: |
            The posture the node's agent last reported (ADR-024). Null until an agent
            has ever connected. Kept after the link drops, so a node that disarmed
            reads DISARMED until an agent reports otherwise.
        agentDisarmReason:
          oneOf:
            - $ref: "#/components/schemas/DisarmReason"
            - type: "null"
          description: Set only while agentPosture is DISARMED.
        approvedAt:
          type: [string, "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time

    NetworkNodePage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/NetworkNode"
        page:
          $ref: "#/components/schemas/PageMeta"

    NetworkNodeReview:
      type: object
      description: |
        One node, and the evidence for and against qualifying it (DV-122).

        Operator-only, and it carries what the public surfaces deliberately do not:
        the owner's identity and the site's exact coordinates. The coordinates are
        here because ADR-013 requires them verified against the sky, and the
        operator cannot compare a plate solve with a number they are not shown.
      additionalProperties: false
      required: [node, owner, site, telescope, evidence, history]
      properties:
        node:
          $ref: "#/components/schemas/NetworkNode"
        owner:
          $ref: "#/components/schemas/NetworkNodeOwner"
        site:
          $ref: "#/components/schemas/NetworkNodeSite"
        telescope:
          description: The node's primary instrument as registered. Null if it has been removed, which also makes the node unbookable.
          oneOf:
            - $ref: "#/components/schemas/RegisterNetworkTelescope"
            - type: "null"
        evidence:
          $ref: "#/components/schemas/NetworkNodeEvidence"
        history:
          type: array
          description: Every state change the node has been through, oldest first.
          items:
            $ref: "#/components/schemas/NetworkNodeHistoryEntry"

    NetworkNodeOwner:
      type: object
      additionalProperties: false
      required: [id, name, email]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email

    NetworkNodeSite:
      type: object
      additionalProperties: false
      required: [latitude, longitude, timezone]
      properties:
        latitude:
          type: number
          minimum: -90
          maximum: 90
        longitude:
          type: number
          minimum: -180
          maximum: 180
        timezone:
          type: string

    NetworkNodeEvidence:
      type: object
      description: |
        What the database can say about ADR-013's conditions. It reports; it does not
        judge. Park proven and the owner's acceptance of terms have no record here
        and remain the operator's attestation on approval.
      additionalProperties: false
      required:
        [safetyEnvelope, horizonMaskEntries, forbiddenAzimuthSectors, completedRealMissions]
      properties:
        safetyEnvelope:
          description: The measurement approval checks for itself. Null when no envelope row exists.
          oneOf:
            - $ref: "#/components/schemas/NetworkNodeEnvelopeEvidence"
            - type: "null"
        horizonMaskEntries:
          type: integer
          minimum: 0
          description: Bearings recorded in the horizon mask. Zero means nothing has been surveyed.
        forbiddenAzimuthSectors:
          type: integer
          minimum: 0
        completedRealMissions:
          type: integer
          minimum: 0
          description: |
            Missions that reached COMPLETE on this observatory in REAL mode. A
            supervised first light is at least one; simulated missions prove nothing
            about the hardware and are not counted.

    NetworkNodeEnvelopeEvidence:
      type: object
      additionalProperties: false
      required: [maxAltitudeDegrees, measuredAt, measuredBy, measurementNote]
      properties:
        maxAltitudeDegrees:
          type: [number, "null"]
          description: Null is UNMEASURED, and approval refuses it.
        measuredAt:
          type: [string, "null"]
          format: date-time
        measuredBy:
          type: [string, "null"]
        measurementNote:
          type: [string, "null"]

    NetworkNodeHistoryEntry:
      type: object
      additionalProperties: false
      required: [action, occurredAt, actorUserId, reason]
      properties:
        action:
          type: string
          enum:
            [REGISTERED, SUBMITTED, APPROVED, SUSPENDED, DEVICE_TOKEN_ISSUED,
             DEVICE_TOKEN_ROTATED, DEVICE_TOKEN_REVOKED]
        occurredAt:
          type: string
          format: date-time
        actorUserId:
          type: [string, "null"]
          format: uuid
        reason:
          type: [string, "null"]
          description: Verbatim, for an approval or a suspension. The operator's own words.

    NetworkNodeList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/NetworkNode"

    RegisterNetworkNodeRequest:
      type: object
      description: |
        The site and the instrument, together. A partner has neither until they
        register, so both are created here.

        There is one `siteName` rather than a name per language. A telescope on a
        roof in Santiago has one name, and manufacturing a Georgian translation of
        it would be inventing data about somebody else's property.
      additionalProperties: false
      required:
        [siteName, city, countryCode, latitude, longitude, timezone, telescope]
      properties:
        siteName:
          type: string
          minLength: 2
          maxLength: 120
        city:
          type: string
          minLength: 1
          maxLength: 120
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        latitude:
          type: number
          minimum: -90
          maximum: 90
        longitude:
          type: number
          minimum: -180
          maximum: 180
        timezone:
          type: string
          minLength: 1
        telescope:
          $ref: "#/components/schemas/RegisterNetworkTelescope"

    RegisterNetworkTelescope:
      type: object
      additionalProperties: false
      required: [name, manufacturer, model, apertureMm, focalLengthMm]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 120
        manufacturer:
          type: string
          minLength: 1
        model:
          type: string
          minLength: 1
        apertureMm:
          type: number
          exclusiveMinimum: 0
        focalLengthMm:
          type: number
          exclusiveMinimum: 0

    ApproveNetworkNodeRequest:
      type: object
      description: |
        ADR-013's qualification, as the operator attests it.

        Every flag must be true. They are separate fields rather than one
        confirmation because they are separate things somebody had to go and do, and
        a single "I confirm" is a box that gets ticked without reading. The measured
        safety envelope is absent from this list deliberately: it is checked against
        the database instead.
      additionalProperties: false
      required:
        [coordinatesVerified, horizonMaskRecorded, firstLightSupervised,
         parkProven, ownerTermsAccepted, reason]
      properties:
        coordinatesVerified:
          type: boolean
          description: The site was confirmed against the sky, by plate solve, rather than typed into a form.
        horizonMaskRecorded:
          type: boolean
          description: The roofline, walls and obstructions at this site are recorded.
        firstLightSupervised:
          type: boolean
          description: At least one full mission was run on this instrument with an operator watching.
        parkProven:
          type: boolean
          description: Park was seen to work on this hardware, both commanded and on link loss.
        ownerTermsAccepted:
          type: boolean
          description: The owner accepted the operating terms in writing, for this node.
        reason:
          type: string
          minLength: 8

    SuspendNetworkNodeRequest:
      type: object
      additionalProperties: false
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 8

    DeviceTokenChangeRequest:
      type: object
      description: Why the operator is issuing, rotating or revoking. Recorded verbatim.
      additionalProperties: false
      required: [reason]
      properties:
        reason:
          type: string
          minLength: 8

    DeviceTokenIssued:
      type: object
      description: |
        The only place a device token ever appears. Give it to the owner for
        `python -m darkview_agent setup`; it cannot be shown again.
      additionalProperties: false
      required: [nodeId, observatoryId, deviceToken, issuedAt]
      properties:
        nodeId:
          type: string
          format: uuid
        observatoryId:
          type: string
          format: uuid
        deviceToken:
          type: string
          description: "Presented by the agent as `Authorization: Bearer`. Never logged."
        issuedAt:
          type: string
          format: date-time

    AuditCategory:
      type: string
      enum:
        - AUTH
        - BOOKING
        - PAYMENT
        - MISSION
        - COMMAND
        - SAFETY
        - OBSERVATORY_MODE
        - OPERATOR_OVERRIDE
        - AGENT_LINK
        - LOYALTY
        - SUBSCRIPTION

    AuditEvent:
      type: object
      description: Append-only. Never edited, never backdated, never fabricated.
      additionalProperties: false
      required: [id, at, category, action]
      properties:
        id:
          type: string
          format: uuid
        at:
          type: string
          format: date-time
        category:
          $ref: "#/components/schemas/AuditCategory"
        action:
          type: string
        actorUserId:
          type: [string, "null"]
          format: uuid
        missionId:
          type: [string, "null"]
          format: uuid
        commandId:
          type: [string, "null"]
          format: uuid
        detail:
          type: object
          additionalProperties: true
          description: Structured detail. Never contains a secret, token or device address.

    AuditEventPage:
      type: object
      additionalProperties: false
      required: [items, page]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AuditEvent"
        page:
          $ref: "#/components/schemas/PageMeta"

    # ---------------------------------------------------------------------------
    # WebSocket: observatory agent link  (/ws/agent)
    # ---------------------------------------------------------------------------

    WebSocketProtocolVersion:
      type: string
      description: Incremented on any breaking change to the agent link message set. The cloud refuses an agent whose version it does not support.
      enum: ["1"]

    AgentHello:
      type: object
      description: First message after the agent dials out and authenticates.
      additionalProperties: false
      required:
        [
          type,
          messageId,
          sentAt,
          protocolVersion,
          observatoryId,
          agentVersion,
          mode,
          posture,
          bootedAt,
        ]
      properties:
        type:
          type: string
          enum: [AGENT_HELLO]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        protocolVersion:
          $ref: "#/components/schemas/WebSocketProtocolVersion"
        observatoryId:
          type: string
          format: uuid
        agentVersion:
          type: string
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        posture:
          $ref: "#/components/schemas/AgentPosture"
        disarmReason:
          oneOf:
            - $ref: "#/components/schemas/DisarmReason"
            - type: "null"
          description: Set only while posture is DISARMED.
        bootedAt:
          type: string
          format: date-time
        safetyEnvelopeConfigured:
          type: boolean
          description: False while maxAltitudeDegrees is unmeasured. The cloud must not schedule a mission against an agent reporting false.
        resumeMissionId:
          type: [string, "null"]
          format: uuid
          description: Set when the agent restarts holding a mission recovered from its local state store.

    AgentHeartbeat:
      type: object
      description: |
        Sent every 5 seconds. Its absence is what triggers the watchdog. Carries the
        agent's posture, so a disarm reaches the cloud within one interval rather than at
        the next reconnect (ADR-024 §5, correction of 2026-09-22).
      additionalProperties: false
      required: [type, messageId, sentAt, sequence, uptimeSeconds, posture]
      properties:
        type:
          type: string
          enum: [AGENT_HEARTBEAT]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        sequence:
          type: integer
          minimum: 0
        uptimeSeconds:
          type: integer
          minimum: 0
        posture:
          $ref: "#/components/schemas/AgentPosture"
        disarmReason:
          oneOf:
            - $ref: "#/components/schemas/DisarmReason"
            - type: "null"
          description: Set only while posture is DISARMED.

    AgentCommandAck:
      type: object
      description: |
        The agent's independent verdict on one command. A REJECTED ack with a SAFETY_
        reason after the cloud approved the command is the safety design working.
      additionalProperties: false
      required: [type, messageId, sentAt, commandId, status]
      properties:
        type:
          type: string
          enum: [AGENT_COMMAND_ACK]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        commandId:
          type: string
          format: uuid
        missionId:
          type: [string, "null"]
          format: uuid
        status:
          $ref: "#/components/schemas/CommandAcceptanceStatus"
        rejectionReason:
          oneOf:
            - $ref: "#/components/schemas/CommandRejectionReason"
            - type: "null"
        detail:
          type: [string, "null"]

    AgentStateDelta:
      type: object
      description: Emitted on the agent control loop. Carries telemetry and, if a mission is held, its state.
      additionalProperties: false
      required: [type, messageId, sentAt, telemetry]
      properties:
        type:
          type: string
          enum: [AGENT_STATE_DELTA]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        telemetry:
          $ref: "#/components/schemas/ObservatoryTelemetry"
        missionId:
          type: [string, "null"]
          format: uuid
        missionState:
          oneOf:
            - $ref: "#/components/schemas/MissionState"
            - type: "null"
        failureReason:
          oneOf:
            - $ref: "#/components/schemas/MissionFailureReason"
            - type: "null"
        centeringIteration:
          type: [integer, "null"]
          minimum: 0
          description: Current plate-solve / re-slew iteration, capped at three.
        residualArcminutes:
          type: [number, "null"]
          description: Distance between the solved position and the target after the last iteration.

    AgentMissionEvent:
      type: object
      description: A state transition the agent wants written to the mission audit trail.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, state, occurredAt]
      properties:
        type:
          type: string
          enum: [AGENT_MISSION_EVENT]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/MissionState"
        failureReason:
          oneOf:
            - $ref: "#/components/schemas/MissionFailureReason"
            - type: "null"
        occurredAt:
          type: string
          format: date-time
          description: When it actually happened on the observatory clock. Replayed unchanged after a reconnect; never rewritten to look contemporaneous.
        commandId:
          type: [string, "null"]
          format: uuid
        detail:
          type: [string, "null"]

    LiveFrameEncoding:
      type: string
      description: Phase 1 delivers MJPEG. WebRTC is a Phase 2 change and requires an approved decision record.
      enum: [JPEG]

    LiveFrameHeader:
      type: object
      description: |
        Metadata for exactly one live-view frame. The encoded bytes follow as the very
        next binary WebSocket frame on the same connection. Pixel data is never
        base64-encoded into JSON.
      additionalProperties: false
      required:
        [
          type,
          messageId,
          sentAt,
          missionId,
          sequence,
          capturedAt,
          encoding,
          widthPx,
          heightPx,
          byteLength,
          exposureMilliseconds,
          gain,
          mode,
        ]
      properties:
        type:
          type: string
          enum: [AGENT_LIVE_FRAME]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        sequence:
          type: integer
          minimum: 0
        capturedAt:
          type: string
          format: date-time
        encoding:
          $ref: "#/components/schemas/LiveFrameEncoding"
        widthPx:
          type: integer
          exclusiveMinimum: 0
        heightPx:
          type: integer
          exclusiveMinimum: 0
        byteLength:
          type: integer
          exclusiveMinimum: 0
          maximum: 4194304
          description: |
            The exact size of the binary frame that follows. Four mebibytes is the
            ceiling, and it is this contract that sets it rather than any one
            transport: a header declaring more is refused before its pixels are
            accepted, and the bytes are never buffered.

            Measured against SimCamera at 1920x1080, the shipped agent defaults of
            a 1024px long edge at quality 70 produce about 165 KB; the largest
            measurement taken was 381 KB at 1280px and quality 82. DV-035
            re-measures both against the real ASI585MC at first light, so this
            leaves an order of magnitude of room above anything yet observed.
        exposureMilliseconds:
          type: number
          exclusiveMinimum: 0
        gain:
          type: integer
          minimum: 0
        stackedFrames:
          type: [integer, "null"]
          exclusiveMinimum: 0
          description: Set when the frame is a live stack rather than a single sub-exposure.
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
          description: A frame produced by SimCamera is marked SIMULATED and any UI showing it must say so.

    AgentCaptureReady:
      type: object
      description: |
        A finished capture has been uploaded by the agent and is ready for the cloud to record.

        Every storage key must be the one the cloud granted for that kind. The cloud
        re-derives each and refuses the whole capture if any differs: a key the
        agent could choose would let it attach another customer's object to a
        capture it reported (DV-065).
      additionalProperties: false
      required:
        [
          type,
          messageId,
          sentAt,
          missionId,
          commandId,
          capturedAt,
          imagingProfile,
          opticalConfig,
          exposureMilliseconds,
          gain,
          framesStacked,
          integrationSeconds,
          imageStorageKey,
          mode,
        ]
      properties:
        type:
          type: string
          enum: [AGENT_CAPTURE_READY]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        commandId:
          type: string
          format: uuid
        capturedAt:
          type: string
          format: date-time
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        opticalConfig:
          $ref: "#/components/schemas/OpticalConfig"
        exposureMilliseconds:
          type: number
          exclusiveMinimum: 0
        gain:
          type: integer
          minimum: 0
        framesStacked:
          type: integer
          exclusiveMinimum: 0
        integrationSeconds:
          type: number
          exclusiveMinimum: 0
        imageStorageKey:
          type: string
          description: Object storage key written by the agent. Not a URL and never a public path.
        unmarkedStorageKey:
          type: [string, "null"]
        fitsStorageKey:
          type: [string, "null"]
        thumbnailStorageKey:
          type: [string, "null"]
          description: |
            The THUMBNAIL the agent wrote, if it wrote one (DV-065). Optional, so an
            agent that predates it still sends a valid message.
        solvedFocalLengthMm:
          type: [number, "null"]
        widthPx:
          type: [integer, "null"]
        heightPx:
          type: [integer, "null"]
        mode:
          $ref: "#/components/schemas/ObservatoryMode"

    AgentErrorSeverity:
      type: string
      enum: [WARNING, ERROR, FATAL]

    AgentError:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, severity, code, message]
      properties:
        type:
          type: string
          enum: [AGENT_ERROR]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        severity:
          $ref: "#/components/schemas/AgentErrorSeverity"
        code:
          type: string
        message:
          type: string
          description: Operator-facing. Never contains a token, credential or local address.
        missionId:
          type: [string, "null"]
          format: uuid
        commandId:
          type: [string, "null"]
          format: uuid

    AgentUploadGrantRequest:
      type: object
      description: |
        The agent asks for somewhere to put one capture asset (ADR-012).

        It does not propose a key. The cloud derives the object's identity from
        facts it already holds, so a compromised agent cannot choose to write over
        another customer's object -- and that is also what lets
        `CaptureAsset.storageKey` be trusted when the capture is finally recorded.

        The mission and command say which capture this is for. Together with
        `kind` they identify the object, so no correlation identifier is needed:
        a grant answers the request naming the same three.
        The agent also declares what it is about to write. A presigned URL signs
        only the headers it was given, so a grant that names neither the media type
        nor the size is a grant to PUT anything of any size at that key until it
        expires. The cloud checks both against the asset kind, signs them, and the
        upload is refused by storage itself if either differs.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, commandId, kind, contentType, contentLength]
      properties:
        type:
          type: string
          enum: [AGENT_UPLOAD_GRANT_REQUEST]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        commandId:
          type: string
          format: uuid
        kind:
          $ref: "#/components/schemas/CaptureAssetKind"
        contentType:
          type: string
          description: |
            Media type of the bytes the agent will PUT. Checked against `kind` and
            then signed, so it is what the agent must actually send.
        contentLength:
          type: integer
          minimum: 1
          description: |
            Exact size in bytes of the object the agent will PUT. Checked against the
            cloud's per-kind maximum and then signed.

    AgentToCloudMessage:
      description: Every message the Observatory Agent may send over its outbound link.
      oneOf:
        - $ref: "#/components/schemas/AgentHello"
        - $ref: "#/components/schemas/AgentHeartbeat"
        - $ref: "#/components/schemas/AgentCommandAck"
        - $ref: "#/components/schemas/AgentStateDelta"
        - $ref: "#/components/schemas/AgentMissionEvent"
        - $ref: "#/components/schemas/LiveFrameHeader"
        - $ref: "#/components/schemas/AgentUploadGrantRequest"
        - $ref: "#/components/schemas/AgentCaptureReady"
        - $ref: "#/components/schemas/AgentError"
      discriminator:
        propertyName: type
        mapping:
          AGENT_HELLO: "#/components/schemas/AgentHello"
          AGENT_HEARTBEAT: "#/components/schemas/AgentHeartbeat"
          AGENT_COMMAND_ACK: "#/components/schemas/AgentCommandAck"
          AGENT_STATE_DELTA: "#/components/schemas/AgentStateDelta"
          AGENT_MISSION_EVENT: "#/components/schemas/AgentMissionEvent"
          AGENT_LIVE_FRAME: "#/components/schemas/LiveFrameHeader"
          AGENT_UPLOAD_GRANT_REQUEST: "#/components/schemas/AgentUploadGrantRequest"
          AGENT_CAPTURE_READY: "#/components/schemas/AgentCaptureReady"
          AGENT_ERROR: "#/components/schemas/AgentError"

    CloudWelcome:
      type: object
      description: Cloud response to AgentHello. Establishes the mission the agent is expected to hold, if any.
      additionalProperties: false
      required: [type, messageId, sentAt, protocolVersion, serverTime]
      properties:
        type:
          type: string
          enum: [CLOUD_WELCOME]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        protocolVersion:
          $ref: "#/components/schemas/WebSocketProtocolVersion"
        serverTime:
          type: string
          format: date-time
          description: For agent clock-skew detection. The agent never uses it to rewrite a recorded event time.
        expectedMissionId:
          type: [string, "null"]
          format: uuid
        heartbeatIntervalSeconds:
          type: integer
          exclusiveMinimum: 0
          default: 5

    CloudCommand:
      type: object
      description: The only way the cloud instructs the observatory.
      additionalProperties: false
      required: [type, messageId, sentAt, command]
      properties:
        type:
          type: string
          enum: [CLOUD_COMMAND]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        command:
          $ref: "#/components/schemas/CommandEnvelope"

    CloudHeartbeatAck:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, sequence]
      properties:
        type:
          type: string
          enum: [CLOUD_HEARTBEAT_ACK]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        sequence:
          type: integer
          minimum: 0

    CloudSessionUpdate:
      type: object
      description: |
        Tells the agent who the single active session owner is. The agent refuses any
        command whose sessionId does not match the value it last received here, which is
        what stops a stale browser tab.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId]
      properties:
        type:
          type: string
          enum: [CLOUD_SESSION_UPDATE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        sessionId:
          type: [string, "null"]
          format: uuid
          description: Null revokes the session. The agent then accepts no client-originated command for this mission.
        userId:
          type: [string, "null"]
          format: uuid
        expiresAt:
          type: [string, "null"]
          format: date-time

    CloudSafetyEnvelopeUpdate:
      type: object
      description: |
        Distributes the measured envelope to the agent. The agent stores it locally and
        keeps enforcing it after the cloud link dies. Receiving an envelope whose
        maxAltitudeDegrees is null puts the agent in the refuse-all-slews state.
      additionalProperties: false
      required: [type, messageId, sentAt, envelope]
      properties:
        type:
          type: string
          enum: [CLOUD_SAFETY_ENVELOPE_UPDATE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        envelope:
          $ref: "#/components/schemas/SafetyEnvelopeConfig"

    CloudWeatherUpdate:
      type: object
      description: |
        Tells the agent what the cloud believes the sky is doing, and whether an
        operator hold stands. The agent stores it locally and keeps enforcing it
        after the link dies, the same way it does the safety envelope: a hold that
        only lived in the cloud would stop meaning anything at the moment the
        observatory most needs it to.

        While `holdActive` is true the agent parks and refuses every command but
        PARK and ABORT, with WEATHER_HOLD_ACTIVE. Phase 1 fits no sky sensor, so
        the only writer is the operator console.
      additionalProperties: false
      required: [type, messageId, sentAt, observatoryId, weather]
      properties:
        type:
          type: string
          enum: [CLOUD_WEATHER_UPDATE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        observatoryId:
          type: string
          format: uuid
        weather:
          $ref: "#/components/schemas/WeatherState"

    CloudOperatingUpdate:
      type: object
      description: |
        The observatory's network node approval status, as the cloud holds it (ADR-024
        §3). Sent on every reconnect and whenever the status changes. It can only ever
        make the agent safer: any status but APPROVED moves an UNATTENDED agent to
        DISARMED, and APPROVED never arms one. Arming is a local act at the observatory.
      additionalProperties: false
      required: [type, messageId, sentAt, observatoryId, approvalStatus]
      properties:
        type:
          type: string
          enum: [CLOUD_OPERATING_UPDATE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        observatoryId:
          type: string
          format: uuid
        approvalStatus:
          $ref: "#/components/schemas/NetworkNodeApprovalStatus"

    CloudError:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, code, message]
      properties:
        type:
          type: string
          enum: [CLOUD_ERROR]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
        fatal:
          type: boolean
          default: false
          description: When true the agent closes the link, backs off and re-dials. It does not stop enforcing safety while disconnected.

    CloudUploadGrant:
      type: object
      description: |
        Permission to write exactly one object, for a few minutes (ADR-012).

        The observatory holds no bucket credential. This URL is the whole of its
        authority over object storage: one key, one method, a short expiry. A
        stolen mini-PC yields a revocable device token and nothing else.

        `storageKey` is what the agent reports back as `imageStorageKey` on
        `AGENT_CAPTURE_READY`. It is the cloud's own derived key, echoed so the
        agent knows what it wrote rather than having to construct it.

        A request the cloud will not grant is answered with `CLOUD_ERROR`, not
        with a grant naming no URL.
      additionalProperties: false
      required:
        [
          type,
          messageId,
          sentAt,
          missionId,
          commandId,
          kind,
          storageKey,
          url,
          method,
          expiresAt,
          contentType,
          contentLength,
        ]
      properties:
        type:
          type: string
          enum: [CLOUD_UPLOAD_GRANT]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        commandId:
          type: string
          format: uuid
        kind:
          $ref: "#/components/schemas/CaptureAssetKind"
        storageKey:
          type: string
          description: Object storage key the cloud derived. Not a URL and never a public path.
        contentType:
          type: string
          description: |
            The media type the URL was signed for, echoed from the request. The agent
            sends exactly this; storage refuses anything else.
        contentLength:
          type: integer
          minimum: 1
          description: |
            The exact size the URL was signed for, echoed from the request. The agent
            sends exactly this many bytes; storage refuses anything else.
        url:
          type: string
          format: uri
          description: Presigned URL. Names one object and one method, and expires.
        method:
          type: string
          description: Always PUT. Stated rather than assumed, so a reader of a captured message knows what it permitted.
          enum: [PUT]
        expiresAt:
          type: string
          format: date-time

    CloudToAgentMessage:
      description: Every message the cloud may send down the agent link.
      oneOf:
        - $ref: "#/components/schemas/CloudWelcome"
        - $ref: "#/components/schemas/CloudCommand"
        - $ref: "#/components/schemas/CloudHeartbeatAck"
        - $ref: "#/components/schemas/CloudSessionUpdate"
        - $ref: "#/components/schemas/CloudSafetyEnvelopeUpdate"
        - $ref: "#/components/schemas/CloudUploadGrant"
        - $ref: "#/components/schemas/CloudWeatherUpdate"
        - $ref: "#/components/schemas/CloudOperatingUpdate"
        - $ref: "#/components/schemas/CloudError"
      discriminator:
        propertyName: type
        mapping:
          CLOUD_WELCOME: "#/components/schemas/CloudWelcome"
          CLOUD_COMMAND: "#/components/schemas/CloudCommand"
          CLOUD_HEARTBEAT_ACK: "#/components/schemas/CloudHeartbeatAck"
          CLOUD_SESSION_UPDATE: "#/components/schemas/CloudSessionUpdate"
          CLOUD_SAFETY_ENVELOPE_UPDATE: "#/components/schemas/CloudSafetyEnvelopeUpdate"
          CLOUD_UPLOAD_GRANT: "#/components/schemas/CloudUploadGrant"
          CLOUD_WEATHER_UPDATE: "#/components/schemas/CloudWeatherUpdate"
          CLOUD_OPERATING_UPDATE: "#/components/schemas/CloudOperatingUpdate"
          CLOUD_ERROR: "#/components/schemas/CloudError"

    # ---------------------------------------------------------------------------
    # WebSocket: mission client channel  (/ws/mission/{missionId})
    # ---------------------------------------------------------------------------

    MissionStateUpdate:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, state]
      properties:
        type:
          type: string
          enum: [MISSION_STATE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        state:
          $ref: "#/components/schemas/MissionState"
        failureReason:
          oneOf:
            - $ref: "#/components/schemas/MissionFailureReason"
            - type: "null"
        remainingSeconds:
          type: [integer, "null"]
          minimum: 0

    MissionTelemetryUpdate:
      type: object
      description: |
        Client-safe telemetry. Deliberately narrower than ObservatoryTelemetry: no
        device identity, no driver state, no address.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, mode, link]
      properties:
        type:
          type: string
          enum: [MISSION_TELEMETRY]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        link:
          $ref: "#/components/schemas/ObservatoryLinkState"
        tracking:
          type: [boolean, "null"]
        centeringIteration:
          type: [integer, "null"]
          minimum: 0
        residualArcminutes:
          type: [number, "null"]
        nudgeUsedDegrees:
          type: [number, "null"]
          minimum: 0
        ambientTemperatureC:
          type: [number, "null"]

    MissionStreamInfo:
      type: object
      description: Where the client reads the live view. A short-expiry signed URL, never a device address.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, streamUrl, encoding, mode, expiresAt]
      properties:
        type:
          type: string
          enum: [MISSION_STREAM]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        streamUrl:
          type: string
          format: uri
        encoding:
          $ref: "#/components/schemas/LiveFrameEncoding"
        mode:
          $ref: "#/components/schemas/ObservatoryMode"
        expiresAt:
          type: string
          format: date-time

    MissionCaptureReady:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, capture]
      properties:
        type:
          type: string
          enum: [MISSION_CAPTURE_READY]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        capture:
          $ref: "#/components/schemas/Capture"

    MissionCommandResult:
      type: object
      description: The outcome of a command the client submitted over HTTP, delivered asynchronously.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, commandId, status]
      properties:
        type:
          type: string
          enum: [MISSION_COMMAND_RESULT]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        commandId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/CommandAcceptanceStatus"
        rejectionReason:
          oneOf:
            - $ref: "#/components/schemas/CommandRejectionReason"
            - type: "null"

    MissionChannelError:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt, code, message]
      properties:
        type:
          type: string
          enum: [MISSION_ERROR]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string

    MissionChannelMessage:
      description: Everything the cloud pushes to a subscribed live-mission client.
      oneOf:
        - $ref: "#/components/schemas/MissionStateUpdate"
        - $ref: "#/components/schemas/MissionTelemetryUpdate"
        - $ref: "#/components/schemas/MissionStreamInfo"
        - $ref: "#/components/schemas/MissionCaptureReady"
        - $ref: "#/components/schemas/MissionCommandResult"
        - $ref: "#/components/schemas/MissionChannelError"
      discriminator:
        propertyName: type
        mapping:
          MISSION_STATE: "#/components/schemas/MissionStateUpdate"
          MISSION_TELEMETRY: "#/components/schemas/MissionTelemetryUpdate"
          MISSION_STREAM: "#/components/schemas/MissionStreamInfo"
          MISSION_CAPTURE_READY: "#/components/schemas/MissionCaptureReady"
          MISSION_COMMAND_RESULT: "#/components/schemas/MissionCommandResult"
          MISSION_ERROR: "#/components/schemas/MissionChannelError"

    MissionClientSubscribe:
      type: object
      description: |
        Ask to watch one mission. Sent by the controller and by an observer, which is
        the only difference `sessionId` carries.
      additionalProperties: false
      required: [type, messageId, sentAt, missionId, sessionId]
      properties:
        type:
          type: string
          enum: [CLIENT_SUBSCRIBE]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time
        missionId:
          type: string
          format: uuid
        sessionId:
          type: [string, "null"]
          format: uuid
          description: |
            The controller's session, or null for an observer (ADR-007).

            The controller states it because a session rotates: reopening replaces the
            old identifier, and a stale browser tab holding the previous one must stop
            being able to watch. An observer has no rotating credential -- the seat is
            the grant and the session cookie verified during the handshake is the
            identity -- so there is nothing for them to state. Required either way, so
            that a client says which it is rather than omitting the field.

            Stating a sessionId never grants anything on its own. The cloud admits a
            subscriber only when the session or the seat belongs to the authenticated
            user, and neither confers any command capability: commands are minted only
            through POST /missions/{missionId}/command, for the session owner alone.

    MissionClientPing:
      type: object
      additionalProperties: false
      required: [type, messageId, sentAt]
      properties:
        type:
          type: string
          enum: [CLIENT_PING]
        messageId:
          type: string
          format: uuid
        sentAt:
          type: string
          format: date-time

    MissionClientMessage:
      description: |
        The complete set of messages a client may send on the mission channel. It is
        deliberately tiny: subscribe and keep-alive. A client may not send a
        CommandEnvelope, a device instruction or any hardware-shaped value here.
      oneOf:
        - $ref: "#/components/schemas/MissionClientSubscribe"
        - $ref: "#/components/schemas/MissionClientPing"
      discriminator:
        propertyName: type
        mapping:
          CLIENT_SUBSCRIBE: "#/components/schemas/MissionClientSubscribe"
          CLIENT_PING: "#/components/schemas/MissionClientPing"

    # ---------------------------------------------------------------------------
    # Admin request bodies
    # ---------------------------------------------------------------------------

    AdminCancelMissionRequest:
      type: object
      additionalProperties: false
      required: [reason, resolution]
      properties:
        reason:
          type: string
          minLength: 4
        resolution:
          type: string
          enum: [REFUND, RESCHEDULE, NONE]

    AdminUpdateTargetRequest:
      type: object
      additionalProperties: false
      properties:
        enabled:
          type: boolean
        imagingProfile:
          $ref: "#/components/schemas/ImagingProfile"
        opticalConfig:
          $ref: "#/components/schemas/OpticalConfig"
        minAltitudeDegrees:
          type: number
          minimum: 0
          maximum: 90
        expectedMissionMinutes:
          type: integer
          exclusiveMinimum: 0
        previewImageUrl:
          type: [string, "null"]
          format: uri
        descriptionEn:
          type: [string, "null"]
        descriptionKa:
          type: [string, "null"]
