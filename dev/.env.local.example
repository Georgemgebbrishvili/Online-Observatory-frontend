# Copy to dev/.env.local (gitignored) and adjust. Read by scripts/dev-stack*.mjs.

# The darkview-platform checkout. Read-only: see CLAUDE.md, "The platform repository".
PLATFORM_DIR=../part-1-platform

# Where the simulated agent believes it is. It computes the Sun from these, locally.
# The seeded demo observatory is in Tbilisi; see dev/README.md, "Daylight", before
# changing them.
SITE_LATITUDE=41.7151
SITE_LONGITUDE=44.8271
